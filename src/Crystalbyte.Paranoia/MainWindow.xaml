<Window x:Class="Crystalbyte.Paranoia.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:contexts="clr-namespace:Crystalbyte.Paranoia.Contexts"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:ui="clr-namespace:Crystalbyte.Paranoia.UI"
        x:Name="Window"
        Title="Paranoia"
        Width="1280"
        Height="768"
        MinWidth="320"
        MinHeight="200"
        WindowStartupLocation="CenterScreen"
        d:DataContext="{d:DesignInstance contexts:AppContext}"
        mc:Ignorable="d">
    <Window.Style>
        <Style TargetType="Window">
            <Setter Property="WindowStyle" Value="None" />
            <Setter Property="ResizeMode" Value="CanResizeWithGrip" />
            <Setter Property="Background" Value="{StaticResource AppWindowBackgroundBrush}" />
            <Setter Property="BorderBrush" Value="{StaticResource AppAccentBrush}" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="WindowChrome.WindowChrome">
                <Setter.Value>
                    <WindowChrome CaptionHeight="48"
                                  GlassFrameThickness="-1"
                                  NonClientFrameEdges="None"
                                  ResizeBorderThickness="0" />
                </Setter.Value>
            </Setter>
            <!--  Windows Shell Integration http://www.seritools.de/programmierung/wpf/wpf-fensterrahmen-anpassen-mit-dem-windowchrome/post503.html  -->
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Window">
                        <Border BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                Padding="{TemplateBinding Padding}">
                            <DockPanel LastChildFill="True">
                                <Grid Background="{StaticResource AppControlBrush}" DockPanel.Dock="Top">
                                    <StackPanel HorizontalAlignment="Right"
                                                VerticalAlignment="Top"
                                                Orientation="Horizontal">
                                        <Button Click="OnMinimizeButtonClicked" Style="{StaticResource MinimizeButtonStyle}" />
                                        <Button Click="OnMaximizeButtonClicked" Style="{StaticResource MaximizeButtonStyle}" />
                                        <Button Click="OnCloseButtonClicked" Style="{StaticResource CloseButtonStyle}" />
                                    </StackPanel>
                                    <StackPanel DockPanel.Dock="Left" Orientation="Horizontal">
                                        <Border>
                                            <TextBlock Margin="6"
                                                       VerticalAlignment="Top"
                                                       Foreground="{StaticResource AppAccentBrush}"
                                                       Text="p4r4n014" />
                                        </Border>
                                        <ListView ItemsSource="{Binding Identities}">
                                            <ListView.ItemContainerStyle>
                                                <Style BasedOn="{StaticResource {x:Type ListViewItem}}"
                                                       TargetType="ListViewItem"
                                                       d:DataContext="{d:DesignInstance contexts:IdentityContext}">
                                                    <Setter Property="IsSelected" Value="{Binding IsSelected, Mode=TwoWay}" />
                                                </Style>
                                            </ListView.ItemContainerStyle>
                                            <ListView.ItemsPanel>
                                                <ItemsPanelTemplate>
                                                    <StackPanel Orientation="Horizontal" />
                                                </ItemsPanelTemplate>
                                            </ListView.ItemsPanel>
                                            <ListView.ItemTemplate>
                                                <DataTemplate DataType="contexts:IdentityContext">
                                                    <Grid Background="Transparent">
                                                        <Image x:Name="Avatar"
                                                               Width="48"
                                                               Height="48"
                                                               SnapsToDevicePixels="True"
                                                               Source="{Binding GravatarUrl,
                                                                                IsAsync=True}" />

                                                        <Popup IsOpen="{Binding IsSelected, Mode=TwoWay}" StaysOpen="False">
                                                            <Border Width="300" Height="100" />
                                                        </Popup>
                                                    </Grid>
                                                </DataTemplate>
                                            </ListView.ItemTemplate>
                                        </ListView>
                                        <Button Command="{Binding CreateIdentityCommand}" Style="{StaticResource CreateIdentityButtonStyle}" />
                                    </StackPanel>
                                </Grid>
                                <ContentPresenter Margin="0,-1,0,0" />
                            </DockPanel>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Style>
    <Window.InputBindings>
        <KeyBinding Key="I"
                    Command="{Binding CreateIdentityCommand}"
                    Modifiers="Control" />
        <KeyBinding Key="N" Modifiers="Control" />
        <KeyBinding Key="R" Modifiers="Control" />
    </Window.InputBindings>
    <DockPanel Background="{StaticResource AppWindowBackgroundBrush}">
        <Border Background="{StaticResource AppControlBrush}" DockPanel.Dock="Bottom">
            <Grid>
                <StackPanel Height="24"
                            Margin="8,0,0,0"
                            Orientation="Horizontal">
                    <TextBlock VerticalAlignment="Center"
                               Style="{StaticResource AppNormalTextStyle}"
                               Text="Syncing ..."
                               Visibility="{Binding IsSyncing,
                                                    Converter={StaticResource BooleanToVisibilityConverter}}" />
                    <TextBlock VerticalAlignment="Center"
                               Style="{StaticResource AppNormalTextStyle}"
                               Text="Ready"
                               Visibility="{Binding IsSyncing,
                                                    ConverterParameter=!,
                                                    Converter={StaticResource BooleanToVisibilityConverter}}" />
                </StackPanel>
                <ResizeGrip HorizontalAlignment="Right"
                            VerticalAlignment="Bottom"
                            Visibility="{Binding IsNormalState,
                                                 ElementName=Window,
                                                 Converter={StaticResource BooleanToVisibilityConverter}}"
                            WindowChrome.ResizeGripDirection="BottomRight" />
            </Grid>
        </Border>
        <Border>
            <Grid>
                <ui:IdentityScreen DataContext="{Binding IdentityScreenContext, Mode=OneTime}" Visibility="{Binding IsActive, Mode=OneWay, Converter={StaticResource BooleanToVisibilityConverter}}" />
            </Grid>
        </Border>
    </DockPanel>
</Window>
