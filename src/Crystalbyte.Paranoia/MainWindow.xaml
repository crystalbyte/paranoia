<Window x:Class="Crystalbyte.Paranoia.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:contexts="clr-namespace:Crystalbyte.Paranoia.Contexts"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:ui="clr-namespace:Crystalbyte.Paranoia.UI"
        x:Name="Window"
        Title="Paranoia"
        Width="1280"
        Height="768"
        MinWidth="800"
        MinHeight="600"
        SnapsToDevicePixels="True"
        UseLayoutRounding="True"
        WindowStartupLocation="CenterScreen"
        d:DataContext="{d:DesignInstance contexts:AppContext}"
        mc:Ignorable="d">
    <Window.Style>
        <Style TargetType="Window">
            <Setter Property="WindowStyle" Value="None" />
            <Setter Property="ResizeMode" Value="CanResizeWithGrip" />
            <Setter Property="Background" Value="{StaticResource AppWindowBackgroundBrush}" />
            <Setter Property="BorderBrush" Value="{StaticResource AppAccentBrush}" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="WindowChrome.WindowChrome">
                <Setter.Value>
                    <WindowChrome CaptionHeight="64"
                                  GlassFrameThickness="-1"
                                  NonClientFrameEdges="None"
                                  ResizeBorderThickness="0" />
                </Setter.Value>
            </Setter>
            <!--  Windows Shell Integration http://www.seritools.de/programmierung/wpf/wpf-fensterrahmen-anpassen-mit-dem-windowchrome/post503.html  -->
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Window">
                        <Border BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                Padding="{TemplateBinding Padding}">
                            <!--  Grid to add more full window items later  -->
                            <Grid>
                                <DockPanel LastChildFill="True">
                                    <Grid Background="{StaticResource AppControlBrush}" DockPanel.Dock="Top">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="100" />
                                            <ColumnDefinition Width="300" />
                                            <ColumnDefinition Width="300" />
                                            <ColumnDefinition Width="*" />
                                        </Grid.ColumnDefinitions>
                                        <StackPanel Grid.Column="3"
                                                    HorizontalAlignment="Right"
                                                    VerticalAlignment="Top"
                                                    Orientation="Horizontal">
                                            <Button Click="OnMinimizeButtonClicked" Style="{StaticResource MinimizeButtonStyle}" />
                                            <Button Click="OnMaximizeButtonClicked" Style="{StaticResource MaximizeButtonStyle}" />
                                            <Button Click="OnCloseButtonClicked" Style="{StaticResource CloseButtonStyle}" />
                                        </StackPanel>
                                        <Border Grid.Column="0">
                                            <TextBlock Margin="6,6,0,0"
                                                       VerticalAlignment="Top"
                                                       Foreground="{StaticResource AppAccentBrush}"
                                                       Text="p4r4n014" />
                                        </Border>
                                        <StackPanel Grid.Column="1" Orientation="Horizontal">
                                            <ListView x:Name="IdentityListView"
                                                      Margin="0,0,0,24"
                                                      ItemsSource="{Binding Identities}"
                                                      SelectedValue="{Binding DataContext,
                                                                              ElementName=ContactListView,
                                                                              Mode=OneWayToSource}"
                                                      SelectionChanged="OnIdentitySelectionChanged"
                                                      Style="{StaticResource AvatarListViewStyle}">
                                                <ListView.ItemContainerStyle>
                                                    <Style BasedOn="{StaticResource AvatarListViewItemStyle}"
                                                           TargetType="ListViewItem"
                                                           d:DataContext="{d:DesignInstance contexts:IdentityContext}">
                                                        <Setter Property="IsSelected" Value="{Binding IsSelected, Mode=TwoWay}" />
                                                        <Setter Property="ToolTip" Value="{Binding Name}" />
                                                    </Style>
                                                </ListView.ItemContainerStyle>
                                                <ListView.ItemsPanel>
                                                    <ItemsPanelTemplate>
                                                        <StackPanel Orientation="Horizontal" />
                                                    </ItemsPanelTemplate>
                                                </ListView.ItemsPanel>
                                                <ListView.ItemTemplate>
                                                    <DataTemplate DataType="contexts:IdentityContext">
                                                        <Grid Background="Transparent">
                                                            <Image x:Name="Avatar"
                                                                   Width="48"
                                                                   Height="48"
                                                                   SnapsToDevicePixels="True"
                                                                   Source="{Binding GravatarUrl,
                                                                                    TargetNullValue={StaticResource GravatarDefaultImageUrl},
                                                                                    FallbackValue={StaticResource GravatarDefaultImageUrl},
                                                                                    IsAsync=True}" />
                                                        </Grid>
                                                    </DataTemplate>
                                                </ListView.ItemTemplate>
                                            </ListView>
                                            <Button VerticalAlignment="Top"
                                                    Command="{Binding CreateIdentityCommand,
                                                                      FallbackValue={StaticResource NullCommand}}"
                                                    Style="{StaticResource CreateIdentityButtonStyle}" />
                                        </StackPanel>
                                        <StackPanel Grid.Column="2" Orientation="Horizontal">
                                            <ListView Margin="0,0,0,24"
                                                      ItemsSource="{Binding ImapAccounts}"
                                                      Style="{StaticResource AvatarListViewStyle}">
                                                <ListView.ItemContainerStyle>
                                                    <Style BasedOn="{StaticResource AvatarListViewItemStyle}"
                                                           TargetType="ListViewItem"
                                                           d:DataContext="{d:DesignInstance contexts:ImapAccountContext}">
                                                        <Setter Property="IsSelected" Value="{Binding IsSelected, Mode=TwoWay}" />
                                                        <Setter Property="ToolTip" Value="{Binding Name}" />
                                                    </Style>
                                                </ListView.ItemContainerStyle>
                                                <ListView.ItemsPanel>
                                                    <ItemsPanelTemplate>
                                                        <StackPanel Orientation="Horizontal" />
                                                    </ItemsPanelTemplate>
                                                </ListView.ItemsPanel>
                                                <ListView.ItemTemplate>
                                                    <DataTemplate DataType="contexts:ImapAccountContext">
                                                        <Grid Background="{StaticResource AppControlBrush}">
                                                            <Image x:Name="Avatar"
                                                                   Width="48"
                                                                   Height="48"
                                                                   SnapsToDevicePixels="True"
                                                                   Source="{Binding Name,
                                                                                    Converter={StaticResource DomainToHostLogoConverter},
                                                                                    IsAsync=True}" />
                                                        </Grid>
                                                    </DataTemplate>
                                                </ListView.ItemTemplate>
                                            </ListView>
                                            <Button VerticalAlignment="Top"
                                                    Command="{Binding CreateAccountCommand}"
                                                    Style="{StaticResource CreateIdentityButtonStyle}" />
                                        </StackPanel>
                                    </Grid>
                                    <ContentPresenter Margin="0,-1,0,0" />
                                </DockPanel>
                            </Grid>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Style>
    <Window.InputBindings>
        <KeyBinding Key="I"
                    Command="{Binding CreateIdentityCommand,
                                      FallbackValue={StaticResource NullCommand}}"
                    Modifiers="Control" />
        <KeyBinding Key="O"
                    Command="{Binding CreateAccountCommand,
                                      FallbackValue={StaticResource NullCommand}}"
                    Modifiers="Control" />
        <KeyBinding Key="N" Modifiers="Control" />
        <KeyBinding Key="R" Modifiers="Control" />
    </Window.InputBindings>
    <DockPanel Background="{StaticResource AppWindowBackgroundBrush}">
        <Border Background="{StaticResource AppControlBrush}" DockPanel.Dock="Bottom">
            <Grid>
                <StackPanel Height="24"
                            Margin="8,0,0,0"
                            Orientation="Horizontal">
                    <TextBlock VerticalAlignment="Center"
                               Style="{StaticResource AppNormalTextStyle}"
                               Text="Syncing ..."
                               Visibility="{Binding IsSyncing,
                                                    FallbackValue=Collapsed,
                                                    Converter={StaticResource BooleanToVisibilityConverter}}" />
                    <TextBlock VerticalAlignment="Center"
                               Style="{StaticResource AppNormalTextStyle}"
                               Text="Ready"
                               Visibility="{Binding IsSyncing,
                                                    ConverterParameter=!,
                                                    FallbackValue=Visible,
                                                    Converter={StaticResource BooleanToVisibilityConverter}}" />
                </StackPanel>
                <ResizeGrip HorizontalAlignment="Right"
                            VerticalAlignment="Bottom"
                            Visibility="{Binding IsNormalState,
                                                 ElementName=Window,
                                                 Converter={StaticResource BooleanToVisibilityConverter}}"
                            WindowChrome.ResizeGripDirection="BottomRight" />
            </Grid>
        </Border>
        <Border>
            <Grid>
                <DockPanel LastChildFill="True">
                    <Border DockPanel.Dock="Left">
                        <Grid>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto" />
                                <RowDefinition Height="*" />
                            </Grid.RowDefinitions>
                            <TextBox Grid.Row="0"
                                     Margin="0"
                                     BorderBrush="Transparent"
                                     BorderThickness="0" />
                            <ListView x:Name="ContactListView"
                                      Grid.Row="1"
                                      ItemsSource="{Binding Contacts}"
                                      SelectionMode="Single" />
                        </Grid>
                    </Border>
                    <Border DockPanel.Dock="Right" />
                    <Border />
                </DockPanel>
                <ui:CreateIdentityScreen DataContext="{Binding CreateIdentityScreenContext}" Visibility="{Binding IsActive, FallbackValue=False, Mode=OneWay, Converter={StaticResource BooleanToVisibilityConverter}}" />
                <ui:CreateAccountScreen DataContext="{Binding CreateAccountScreenContext}" Visibility="{Binding IsActive, FallbackValue=False, Mode=OneWay, Converter={StaticResource BooleanToVisibilityConverter}}" />
            </Grid>
        </Border>
    </DockPanel>
</Window>