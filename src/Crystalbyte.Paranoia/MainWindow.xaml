<ui:MetroWindow x:Class="Crystalbyte.Paranoia.MainWindow"
                xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                xmlns:componentModel="clr-namespace:System.ComponentModel;assembly=WindowsBase"
                xmlns:converters="clr-namespace:Crystalbyte.Paranoia.UI.Converters"
                xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
                xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
                xmlns:paranoia="clr-namespace:Crystalbyte.Paranoia"
                xmlns:properties="clr-namespace:Crystalbyte.Paranoia.Properties"
                xmlns:ui="clr-namespace:Crystalbyte.Paranoia.UI"
                x:Name="Window"
                Title="p4r4n014"
                Width="1280"
                Height="768"
                MinWidth="1024"
                MinHeight="620"
                BorderThickness="0"
                KeyboardNavigation.TabNavigation="Local"
                UseLayoutRounding="True"
                WindowStartupLocation="CenterScreen"
                d:DataContext="{d:DesignInstance paranoia:AppContext}"
                mc:Ignorable="d">
    <ui:MetroWindow.Background>
        <VisualBrush TileMode="Tile" Viewport="0,0,1,1">
            <VisualBrush.Visual>
                <Image Source="/Assets/map.jpg" />
            </VisualBrush.Visual>
        </VisualBrush>
    </ui:MetroWindow.Background>
    <ui:MetroWindow.Resources>
        <!--  ReSharper disable once Xaml.RedundantResource  -->
        <Storyboard x:Key="OverlaySlideInStoryboard">
            <DoubleAnimationUsingKeyFrames Storyboard.TargetName="Overlay" Storyboard.TargetProperty="(UIElement.Opacity)">
                <EasingDoubleKeyFrame KeyTime="0" Value="0" />
                <EasingDoubleKeyFrame KeyTime="0:0:0.3" Value="1">
                    <EasingDoubleKeyFrame.EasingFunction>
                        <QuadraticEase EasingMode="EaseOut" />
                    </EasingDoubleKeyFrame.EasingFunction>
                </EasingDoubleKeyFrame>
            </DoubleAnimationUsingKeyFrames>
            <DoubleAnimationUsingKeyFrames Storyboard.TargetName="OverlayContent" Storyboard.TargetProperty="(UIElement.RenderTransform).(TransformGroup.Children)[3].(TranslateTransform.X)">
                <EasingDoubleKeyFrame KeyTime="0" Value="100" />
                <EasingDoubleKeyFrame KeyTime="0:0:0.3" Value="0">
                    <EasingDoubleKeyFrame.EasingFunction>
                        <QuinticEase EasingMode="EaseOut" />
                    </EasingDoubleKeyFrame.EasingFunction>
                </EasingDoubleKeyFrame>
            </DoubleAnimationUsingKeyFrames>
            <DoubleAnimationUsingKeyFrames Storyboard.TargetName="Button" Storyboard.TargetProperty="(UIElement.RenderTransform).(TransformGroup.Children)[3].(TranslateTransform.X)">
                <EasingDoubleKeyFrame KeyTime="0" Value="40" />
                <EasingDoubleKeyFrame KeyTime="0:0:0.3" Value="0">
                    <EasingDoubleKeyFrame.EasingFunction>
                        <QuinticEase EasingMode="EaseOut" />
                    </EasingDoubleKeyFrame.EasingFunction>
                </EasingDoubleKeyFrame>
            </DoubleAnimationUsingKeyFrames>
            <DoubleAnimationUsingKeyFrames Storyboard.TargetName="TextBlock" Storyboard.TargetProperty="(UIElement.RenderTransform).(TransformGroup.Children)[3].(TranslateTransform.X)">
                <EasingDoubleKeyFrame KeyTime="0" Value="80" />
                <EasingDoubleKeyFrame KeyTime="0:0:0.3" Value="0">
                    <EasingDoubleKeyFrame.EasingFunction>
                        <QuinticEase EasingMode="EaseOut" />
                    </EasingDoubleKeyFrame.EasingFunction>
                </EasingDoubleKeyFrame>
            </DoubleAnimationUsingKeyFrames>
        </Storyboard>
        <!--  ReSharper disable once Xaml.RedundantResource  -->
        <Storyboard x:Key="OverlaySlideOutStoryboard">
            <DoubleAnimationUsingKeyFrames Storyboard.TargetName="Overlay" Storyboard.TargetProperty="(UIElement.Opacity)">
                <EasingDoubleKeyFrame KeyTime="0" Value="1" />
                <EasingDoubleKeyFrame KeyTime="0:0:0.3" Value="0">
                    <EasingDoubleKeyFrame.EasingFunction>
                        <QuadraticEase EasingMode="EaseOut" />
                    </EasingDoubleKeyFrame.EasingFunction>
                </EasingDoubleKeyFrame>
            </DoubleAnimationUsingKeyFrames>
            <DoubleAnimationUsingKeyFrames Storyboard.TargetName="OverlayContent" Storyboard.TargetProperty="(UIElement.RenderTransform).(TransformGroup.Children)[3].(TranslateTransform.X)">
                <EasingDoubleKeyFrame KeyTime="0" Value="0" />
                <EasingDoubleKeyFrame KeyTime="0:0:0.3" Value="100">
                    <EasingDoubleKeyFrame.EasingFunction>
                        <QuinticEase EasingMode="EaseOut" />
                    </EasingDoubleKeyFrame.EasingFunction>
                </EasingDoubleKeyFrame>
            </DoubleAnimationUsingKeyFrames>
            <DoubleAnimationUsingKeyFrames Storyboard.TargetName="Button" Storyboard.TargetProperty="(UIElement.RenderTransform).(TransformGroup.Children)[3].(TranslateTransform.X)">
                <EasingDoubleKeyFrame KeyTime="0" Value="0" />
                <EasingDoubleKeyFrame KeyTime="0:0:0.3" Value="40">
                    <EasingDoubleKeyFrame.EasingFunction>
                        <QuinticEase EasingMode="EaseOut" />
                    </EasingDoubleKeyFrame.EasingFunction>
                </EasingDoubleKeyFrame>
            </DoubleAnimationUsingKeyFrames>
            <DoubleAnimationUsingKeyFrames Storyboard.TargetName="TextBlock" Storyboard.TargetProperty="(UIElement.RenderTransform).(TransformGroup.Children)[3].(TranslateTransform.X)">
                <EasingDoubleKeyFrame KeyTime="0" Value="0" />
                <EasingDoubleKeyFrame KeyTime="0:0:0.3" Value="80">
                    <EasingDoubleKeyFrame.EasingFunction>
                        <QuinticEase EasingMode="EaseOut" />
                    </EasingDoubleKeyFrame.EasingFunction>
                </EasingDoubleKeyFrame>
            </DoubleAnimationUsingKeyFrames>
        </Storyboard>
        <converters:BooleanToBrushConverter x:Key="SubjectBrushConverter"
                                            BrushForFalse="{StaticResource AppLightTextBrush}"
                                            BrushForTrue="{StaticResource AppMediumTextBrush}" />
    </ui:MetroWindow.Resources>
    <ui:MetroWindow.InputBindings>
        <KeyBinding Key="N"
                    Command="{Binding WriteMessageCommand}"
                    Modifiers="Control" />
        <KeyBinding Key="P"
                    Command="{Binding PrintCommand}"
                    Modifiers="Control" />
        <KeyBinding Key="Escape" Command="ui:WindowCommands.CloseFlyOut" />
    </ui:MetroWindow.InputBindings>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" MinHeight="32" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="4" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="320"
                              MinWidth="280"
                              MaxWidth="500" />
            <ColumnDefinition Width="Auto" />
            <ColumnDefinition Width="400"
                              MinWidth="260"
                              MaxWidth="800" />
            <ColumnDefinition Width="Auto" />
            <ColumnDefinition Width="0.55*" />
            <ColumnDefinition Width="Auto" />
        </Grid.ColumnDefinitions>
        <Grid.Resources>
            <CollectionViewSource x:Key="MessagesSource" Source="{Binding Messages}">
                <CollectionViewSource.SortDescriptions>
                    <componentModel:SortDescription Direction="Descending" PropertyName="EntryDate" />
                </CollectionViewSource.SortDescriptions>
            </CollectionViewSource>
        </Grid.Resources>
        <DockPanel Grid.Row="0"
                   Grid.Column="0"
                   Margin="6,0,0,0"
                   VerticalAlignment="Center"
                   Panel.ZIndex="1000"
                   LastChildFill="True"
                   d:IsLocked="True">
            <Image Height="20"
                   Margin="0,0,6,0"
                   VerticalAlignment="Center"
                   DockPanel.Dock="Left"
                   Source="{Binding Icon,
                                    RelativeSource={RelativeSource FindAncestor,
                                                                   AncestorType=paranoia:MainWindow}}" />
            <TextBlock Margin="0,-2,0,0"
                       VerticalAlignment="Center"
                       FontSize="17"
                       Style="{DynamicResource {ComponentResourceKey {x:Type properties:Resources},
                                                                     AppTitleTextStyle}}"
                       Text="{Binding Title,
                                      RelativeSource={RelativeSource FindAncestor,
                                                                     AncestorType=paranoia:MainWindow}}" />
        </DockPanel>
        <Grid Grid.Row="0"
              Grid.Column="5"
              HorizontalAlignment="Right"
              VerticalAlignment="Top"
              Panel.ZIndex="2000">
            <Grid.ColumnDefinitions>
                <!--  help button  -->
                <ColumnDefinition Width="Auto" />
                <!--  minimize button  -->
                <ColumnDefinition Width="Auto" />
                <!--  restore down button  -->
                <ColumnDefinition Width="Auto" />
                <!--  maximize down button  -->
                <ColumnDefinition Width="Auto" />
                <!--  close button  -->
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <Button Grid.Column="0" Style="{DynamicResource {ComponentResourceKey {x:Type properties:Resources}, HelpButtonStyle}}" />
            <Button Grid.Column="1" Style="{DynamicResource {ComponentResourceKey {x:Type properties:Resources}, MinimizeButtonStyle}}" />
            <Button Grid.Column="2"
                    Style="{DynamicResource {ComponentResourceKey {x:Type properties:Resources},
                                                                  MaximizeButtonStyle}}"
                    Visibility="{Binding IsNormalized,
                                         Converter={StaticResource BooleanToVisibilityConverter},
                                         RelativeSource={RelativeSource FindAncestor,
                                                                        AncestorType=paranoia:MainWindow}}" />
            <Button Grid.Column="3"
                    Style="{DynamicResource {ComponentResourceKey {x:Type properties:Resources},
                                                                  RestoreDownButtonStyle}}"
                    Visibility="{Binding IsMaximized,
                                         Converter={StaticResource BooleanToVisibilityConverter},
                                         RelativeSource={RelativeSource FindAncestor,
                                                                        AncestorType=paranoia:MainWindow}}" />
            <Button Grid.Column="4" Style="{DynamicResource {ComponentResourceKey {x:Type properties:Resources}, CloseButtonStyle}}" />
        </Grid>
        <Grid Grid.Row="3"
              Grid.Column="0"
              Panel.ZIndex="200">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="4" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>
            <Rectangle Grid.RowSpan="3"
                       Margin="0"
                       Fill="{DynamicResource {ComponentResourceKey {x:Type properties:Resources},
                                                                    AppShadeBrush}}"
                       Opacity="{DynamicResource {ComponentResourceKey {x:Type properties:Resources},
                                                                       AppShadeOpacity}}"
                       RenderOptions.EdgeMode="Aliased" />

            <Rectangle Grid.Row="4"
                       Grid.RowSpan="2"
                       Fill="{DynamicResource {ComponentResourceKey {x:Type properties:Resources},
                                                                    AppShadeBrush}}"
                       Opacity="{DynamicResource {ComponentResourceKey {x:Type properties:Resources},
                                                                       AppShadeOpacity}}"
                       RenderOptions.EdgeMode="Aliased" />
            <DockPanel Grid.Row="0"
                       Grid.Column="0"
                       Margin="16,8,8,8"
                       VerticalAlignment="Stretch">
                <Image Width="100"
                       Height="100"
                       Margin="0,0,8,0"
                       DataContext="{Binding SelectedAccount}"
                       Source="{Binding Address,
                                        Converter={StaticResource GravatarImageConverter},
                                        IsAsync=True}"
                       Stretch="Fill" />
                <Grid VerticalAlignment="Bottom">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                    </Grid.RowDefinitions>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="Auto" />
                        <ColumnDefinition Width="Auto" />
                        <ColumnDefinition Width="Auto" />
                        <ColumnDefinition Width="Auto" />
                    </Grid.ColumnDefinitions>
                    <ui:MetroCircleButton Grid.Row="0"
                                          Grid.Column="1"
                                          Margin="6"
                                          HorizontalAlignment="Center"
                                          VerticalAlignment="Center"
                                          Command="{Binding CreateAccountCommand}">
                        <Image Width="18" Source="/Assets/add.png" />
                    </ui:MetroCircleButton>
                    <ui:MetroCircleButton Grid.Row="0"
                                          Grid.Column="2"
                                          Margin="6"
                                          HorizontalAlignment="Center"
                                          VerticalAlignment="Center"
                                          Command="{Binding ConfigAccountCommand}"
                                          IsEnabled="{Binding IsAccountSelected}"
                                          Visibility="{Binding SelectedAccount,
                                                               Converter={StaticResource NullToVisibilityConverter}}">
                        <Image Width="18" Source="/Assets/settings.png" />
                    </ui:MetroCircleButton>
                    <ui:MetroCircleButton x:Name="SelectAccountButton"
                                          Grid.Row="0"
                                          Grid.Column="3"
                                          Margin="6"
                                          HorizontalAlignment="Center"
                                          VerticalAlignment="Center"
                                          Command="{Binding SelectAccountCommand}"
                                          IsEnabled="{Binding CanSwitchAccounts}"
                                          ToolTip="{x:Static properties:Resources.SelectAccountTooltip}"
                                          Visibility="{Binding SelectedAccount,
                                                               Converter={StaticResource NullToVisibilityConverter}}">
                        <Image Width="18" Source="/Assets/select.user.png" />
                    </ui:MetroCircleButton>
                    <ui:MetroCircleButton Grid.Row="0"
                                          Grid.Column="4"
                                          Margin="6"
                                          HorizontalAlignment="Center"
                                          VerticalAlignment="Center"
                                          Command="{Binding DeleteAccountCommand}"
                                          Visibility="{Binding SelectedAccount,
                                                               Converter={StaticResource NullToVisibilityConverter}}">
                        <Image Width="18" Source="/Assets/delete.png" />
                    </ui:MetroCircleButton>
                    <Popup x:Name="AccountSelectionPopup"
                           Grid.Row="0"
                           Grid.Column="0"
                           AllowsTransparency="True"
                           IsOpen="{Binding IsAccountSelectionRequested}"
                           Placement="Bottom"
                           PlacementTarget="{Binding ElementName=SelectAccountButton}"
                           PopupAnimation="None"
                           StaysOpen="False"
                           VerticalOffset="10">
                        <Border Height="200"
                                Margin="8"
                                Background="{DynamicResource {ComponentResourceKey {x:Type properties:Resources},
                                                                                   AppMenuBackgroundBrush}}"
                                BorderBrush="{DynamicResource {ComponentResourceKey {x:Type properties:Resources},
                                                                                    AppAccentBrush}}"
                                BorderThickness="1">
                            <Border.Effect>
                                <DropShadowEffect />
                            </Border.Effect>
                            <ListView Margin="1"
                                      BorderThickness="0"
                                      ItemsSource="{Binding Accounts}"
                                      SelectedValue="{Binding SelectedAccount,
                                                              Mode=TwoWay}"
                                      SelectionChanged="OnSelectedAccountChanged">
                                <ListView.ItemTemplate>
                                    <DataTemplate DataType="paranoia:MailAccountContext">
                                        <StackPanel Orientation="Horizontal">
                                            <Image Width="32"
                                                   Height="32"
                                                   RenderOptions.BitmapScalingMode="HighQuality"
                                                   Source="{Binding Address,
                                                                    IsAsync=True,
                                                                    Converter={StaticResource GravatarImageConverter},
                                                                    ConverterParameter=32}"
                                                   Stretch="None" />
                                            <TextBlock Margin="8,0,8,0"
                                                       VerticalAlignment="Center"
                                                       FontSize="{DynamicResource {ComponentResourceKey {x:Type properties:Resources},
                                                                                                        AppMediumFontSize}}"
                                                       FontWeight="Normal"
                                                       Text="{Binding Name}" />
                                        </StackPanel>
                                    </DataTemplate>
                                </ListView.ItemTemplate>
                                <ListView.ItemContainerStyle>
                                    <Style BasedOn="{StaticResource {x:Type ListViewItem}}" TargetType="ListViewItem">
                                        <!--  ReSharper disable once Xaml.BindingWithContextNotResolved  -->
                                        <Setter Property="IsSelected" Value="{Binding IsSelected, Mode=TwoWay}" />
                                    </Style>
                                </ListView.ItemContainerStyle>
                            </ListView>
                        </Border>
                    </Popup>
                </Grid>
            </DockPanel>
            <TextBlock Grid.Row="1"
                       Grid.Column="0"
                       Margin="16,0,0,0"
                       DataContext="{Binding SelectedAccount}"
                       Style="{DynamicResource {ComponentResourceKey {x:Type properties:Resources},
                                                                     AppTitleTextStyle}}"
                       Text="{Binding Name}"
                       TextTrimming="CharacterEllipsis"
                       TextWrapping="NoWrap" />
            <TextBlock Grid.Row="2"
                       Grid.Column="0"
                       Margin="16,0,0,16"
                       DataContext="{Binding SelectedAccount}"
                       Style="{DynamicResource {ComponentResourceKey {x:Type properties:Resources},
                                                                     AppSubtitleTextStyle}}"
                       Text="{Binding Address}"
                       TextTrimming="CharacterEllipsis" />

            <Border Grid.Row="4"
                    Grid.Column="0"
                    Margin="16,0,8,0"
                    Padding="0">
                <DockPanel LastChildFill="False">
                    <ui:MetroCircleButton Margin="6"
                                          HorizontalAlignment="Center"
                                          VerticalAlignment="Center"
                                          Command="{Binding WriteMessageCommand}"
                                          DockPanel.Dock="Right">
                        <Image Width="18" Source="Assets/message.png" />
                    </ui:MetroCircleButton>
                    <TextBlock VerticalAlignment="Center"
                               DataContext="{Binding SelectedAccount}"
                               DockPanel.Dock="Left"
                               FontSize="{DynamicResource {ComponentResourceKey {x:Type properties:Resources},
                                                                                AppExtraLargeFontSize}}"
                               Style="{DynamicResource {ComponentResourceKey {x:Type properties:Resources},
                                                                             AppTitleTextStyle}}">
                        <Run Text="{x:Static properties:Resources.ContactsTitle}" />
                        <Run Foreground="{DynamicResource {ComponentResourceKey {x:Type properties:Resources}, AppMediumTextBrush}}" Text="{Binding Contacts, Mode=OneWay, StringFormat={}({0}), Converter={StaticResource CollectionCounter}, FallbackValue=(0)}" />
                    </TextBlock>
                </DockPanel>

            </Border>
            <ListView Grid.Row="5"
                      Grid.Column="0"
                      Margin="0"
                      Background="Transparent"
                      BorderThickness="0"
                      DataContext="{Binding SelectedAccount}"
                      ItemsSource="{Binding Contacts}"
                      KeyboardNavigation.DirectionalNavigation="Contained"
                      KeyboardNavigation.TabNavigation="None"
                      SelectedValue="{Binding SelectedContact,
                                              Mode=TwoWay}"
                      TabIndex="0"
                      d:DataContext="{d:DesignInstance {x:Type paranoia:MailAccountContext}}">
                <ListView.ItemsPanel>
                    <ItemsPanelTemplate>
                        <VirtualizingStackPanel x:Name="Cvsp" />
                    </ItemsPanelTemplate>
                </ListView.ItemsPanel>
                <ListView.ItemContainerStyle>
                    <Style BasedOn="{StaticResource {x:Type ListViewItem}}" TargetType="ListViewItem">
                        <Setter Property="IsSelected" Value="{Binding IsSelected, Mode=TwoWay}" />
                        <Setter Property="Width" Value="{Binding ActualWidth, ElementName=Cvsp}" />
                        <Setter Property="Padding" Value="6,2,6,1" />
                    </Style>
                </ListView.ItemContainerStyle>
                <ListView.ItemTemplate>
                    <DataTemplate DataType="paranoia:MailContactContext">
                        <Grid>
                            <Grid.RowDefinitions>
                                <RowDefinition />
                                <RowDefinition />
                            </Grid.RowDefinitions>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto" />
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="Auto" />
                                <ColumnDefinition Width="Auto" />
                                <ColumnDefinition Width="Auto" />
                            </Grid.ColumnDefinitions>
                            <Image Grid.Row="0"
                                   Grid.RowSpan="2"
                                   Grid.Column="0"
                                   Width="40"
                                   Margin="0,0,6,0"
                                   VerticalAlignment="Center"
                                   RenderOptions.BitmapScalingMode="Fant"
                                   Source="{Binding Address,
                                                    Converter={StaticResource GravatarImageConverter}}" />
                            <TextBlock Grid.Row="0"
                                       Grid.Column="1"
                                       FontSize="{DynamicResource {ComponentResourceKey {x:Type properties:Resources},
                                                                                        AppMediumLargeFontSize}}"
                                       FontWeight="Thin"
                                       Foreground="{DynamicResource {ComponentResourceKey {x:Type properties:Resources},
                                                                                          AppLightTextBrush}}"
                                       RenderOptions.ClearTypeHint="Enabled"
                                       Text="{Binding Name}" />
                            <TextBlock x:Name="AddressBlock"
                                       Grid.Row="1"
                                       Grid.Column="1"
                                       FontWeight="Thin"
                                       RenderOptions.ClearTypeHint="Enabled"
                                       Text="{Binding Address}">

                                <TextBlock.Foreground>
                                    <Binding Mode="OneWay" Path="HasUnseenMessages">
                                        <Binding.Converter>
                                            <converters:BooleanToBrushConverter BrushForFalse="{DynamicResource {ComponentResourceKey {x:Type properties:Resources}, AppMediumTextBrush}}" BrushForTrue="{DynamicResource {ComponentResourceKey {x:Type properties:Resources}, AppAccentBrush}}" />
                                        </Binding.Converter>
                                    </Binding>
                                </TextBlock.Foreground>

                            </TextBlock>
                            <StackPanel Grid.Row="0"
                                        Grid.RowSpan="2"
                                        Grid.Column="2"
                                        Margin="6,0,6,0"
                                        VerticalAlignment="Center"
                                        Visibility="{Binding NotSeenCount,
                                                             Converter={StaticResource QuantityToVisibilityConverter}}">
                                <TextBlock FontSize="{DynamicResource {ComponentResourceKey {x:Type properties:Resources},
                                                                                            AppMediumLargeFontSize}}"
                                           Foreground="{DynamicResource {ComponentResourceKey {x:Type properties:Resources},
                                                                                              AppAccentBrush}}"
                                           Text="{Binding NotSeenCount}" />
                            </StackPanel>
                            <Image Grid.Row="0"
                                   Grid.RowSpan="2"
                                   Grid.Column="3"
                                   Source="/Assets/security.checked.png"
                                   Style="{StaticResource AppSecurityImageStyle}" />
                            <Image Grid.Row="0"
                                   Grid.RowSpan="2"
                                   Grid.Column="4"
                                   Source="/Assets/user.checked.png"
                                   Style="{StaticResource AppSecurityImageStyle}">
                                <Image.Opacity>
                                    <Binding Mode="OneWay" Path="IsValidated">
                                        <Binding.Converter>
                                            <converters:BooleanToOpacityConverter OpacityForFalse=".1" OpacityForTrue="1" />
                                        </Binding.Converter>
                                    </Binding>
                                </Image.Opacity>
                            </Image>
                        </Grid>
                        <DataTemplate.Triggers>
                            <DataTrigger Binding="{Binding IsSelected}" Value="True">
                                <Setter TargetName="AddressBlock" Property="Foreground" Value="{DynamicResource {ComponentResourceKey {x:Type properties:Resources}, AppLightTextBrush}}" />
                            </DataTrigger>
                        </DataTemplate.Triggers>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>
        </Grid>
        <GridSplitter Grid.Row="1"
                      Grid.RowSpan="3"
                      Grid.Column="1" />
        <!--
            <StackPanel Grid.Row="0"
            Grid.Column="2"
            Grid.ColumnSpan="2"
            Margin="0"
            VerticalAlignment="Center"
            Orientation="Horizontal" d:IsHidden="True" d:IsLocked="True">
            <TextBox x:Name="SearchBox"
            Margin="0,4,0,4"
            TabIndex="3"
            Text="{Binding QueryString,
            Mode=TwoWay,
            UpdateSourceTrigger=PropertyChanged}"
            ui:Watermark.IsEnabled="True"
            ui:Watermark.Text="{x:Static properties:Resources.SearchBoxWatermark}">
            <TextBox.InputBindings>
            <KeyBinding Key="Escape"
            Command="{Binding FocusSearchBoxCommand}"
            CommandParameter="!" />
            </TextBox.InputBindings>
            </TextBox>
            </StackPanel>
        -->
        <!--
            <Rectangle Grid.Row="1"
            Grid.Column="0"
            Grid.ColumnSpan="5"
            Margin="0"
            Fill="{DynamicResource {ComponentResourceKey {x:Type properties:Resources},
            AppShadeBrush}}"
            Opacity="{DynamicResource {ComponentResourceKey {x:Type properties:Resources},
            AppShadeOpacity}}"
            RenderOptions.EdgeMode="Aliased" />
        -->
        <Grid Grid.Row="0"
              Grid.Column="2"
              Grid.ColumnSpan="3"
              Margin="0">
            <ListView HorizontalContentAlignment="Stretch"
                      Background="Transparent"
                      BorderThickness="0"
                      DataContext="{Binding SelectedAccount}"
                      ItemsSource="{Binding Mailboxes}"
                      KeyboardNavigation.DirectionalNavigation="Contained"
                      KeyboardNavigation.TabNavigation="None"
                      Padding="0"
                      SelectedValue="{Binding SelectedMailbox,
                                              Mode=TwoWay}"
                      TabIndex="2"
                      d:DataContext="{d:DesignInstance {x:Type paranoia:MailAccountContext}}">
                <ListView.InputBindings>
                    <KeyBinding Key="Delete" Command="{Binding DropMailboxCommand}" />
                </ListView.InputBindings>
                <ListView.ItemContainerStyle>
                    <Style BasedOn="{StaticResource {x:Type ListViewItem}}" TargetType="ListViewItem">
                        <Setter Property="Margin" Value="0" />
                        <Setter Property="Padding" Value="8,1,8,1" />
                        <Setter Property="IsSelected" Value="{Binding IsSelected, Mode=TwoWay}" />
                    </Style>
                </ListView.ItemContainerStyle>
                <ListView.ItemsPanel>
                    <ItemsPanelTemplate>
                        <StackPanel Orientation="Horizontal" />
                    </ItemsPanelTemplate>
                </ListView.ItemsPanel>
                <ListView.ItemTemplate>
                    <DataTemplate DataType="paranoia:MailboxContext">
                        <Grid x:Name="MailboxLayoutRoot">
                            <StackPanel Orientation="Horizontal">
                                <TextBlock Margin="3"
                                           VerticalAlignment="Center"
                                           FontSize="{DynamicResource {ComponentResourceKey {x:Type properties:Resources},
                                                                                            AppMediumLargeFontSize}}"
                                           FontWeight="Light"
                                           RenderOptions.ClearTypeHint="Enabled"
                                           Style="{DynamicResource {ComponentResourceKey {x:Type properties:Resources},
                                                                                         AppLightTextStyle}}"
                                           Text="{Binding Type,
                                                          Converter={StaticResource MailboxLocalizer}}">
                                    <TextBlock.Foreground>
                                        <Binding Path="IsAssigned">
                                            <Binding.Converter>
                                                <converters:BooleanToBrushConverter BrushForFalse="{DynamicResource {ComponentResourceKey {x:Type properties:Resources}, AppDisabledTextBrush}}" BrushForTrue="{DynamicResource {ComponentResourceKey {x:Type properties:Resources}, AppLightTextBrush}}" />
                                            </Binding.Converter>
                                        </Binding>
                                    </TextBlock.Foreground>
                                </TextBlock>
                                <TextBlock x:Name="QuantityBlock"
                                           VerticalAlignment="Center"
                                           FontSize="{DynamicResource {ComponentResourceKey {x:Type properties:Resources},
                                                                                            AppMediumLargeFontSize}}"
                                           Style="{DynamicResource {ComponentResourceKey {x:Type properties:Resources},
                                                                                         AppLightTextStyle}}"
                                           Text="{Binding NotSeenCount,
                                                          StringFormat={}({0})}"
                                           Visibility="{Binding NotSeenCount,
                                                                Converter={StaticResource QuantityToVisibilityConverter}}">

                                    <TextBlock.Foreground>
                                        <Binding Path="IsSelected">
                                            <Binding.Converter>
                                                <converters:BooleanToBrushConverter BrushForFalse="{DynamicResource {ComponentResourceKey {x:Type properties:Resources}, AppAccentBrush}}" BrushForTrue="{DynamicResource {ComponentResourceKey {x:Type properties:Resources}, AppLightTextBrush}}" />
                                            </Binding.Converter>
                                        </Binding>
                                    </TextBlock.Foreground>
                                </TextBlock>
                            </StackPanel>
                            <Popup x:Name="AssignmentPopup"
                                   Grid.Row="0"
                                   Grid.Column="0"
                                   AllowsTransparency="True"
                                   IsOpen="False"
                                   Placement="Center"
                                   PlacementTarget="{Binding ElementName=MailboxLayoutRoot}"
                                   StaysOpen="False"
                                   VerticalOffset="320">
                                <StackPanel Width="300"
                                            Height="600"
                                            Margin="0,0,8,8">
                                    <StackPanel.Effect>
                                        <DropShadowEffect />
                                    </StackPanel.Effect>
                                    <Polygon Fill="{DynamicResource {ComponentResourceKey {x:Type properties:Resources},
                                                                                          AppShadeBrush}}"
                                             Points="150,0 140,12 160,12"
                                             Stroke="{DynamicResource {ComponentResourceKey {x:Type properties:Resources},
                                                                                            AppAccentBrush}}" />
                                    <Border Margin="0,-1,0,0"
                                            Background="{DynamicResource {ComponentResourceKey {x:Type properties:Resources},
                                                                                               AppShadeBrush}}"
                                            BorderBrush="{DynamicResource {ComponentResourceKey {x:Type properties:Resources},
                                                                                                AppAccentBrush}}"
                                            BorderThickness="1">
                                        <Grid>
                                            <Grid.RowDefinitions>
                                                <RowDefinition Height="Auto" />
                                                <RowDefinition Height="*" />
                                                <RowDefinition Height="Auto" />
                                            </Grid.RowDefinitions>
                                            <Rectangle Grid.Row="0"
                                                       Width="18.5"
                                                       Height="2"
                                                       Margin="0,0,0,0"
                                                       HorizontalAlignment="Center"
                                                       VerticalAlignment="Top"
                                                       Panel.ZIndex="500"
                                                       Fill="{DynamicResource {ComponentResourceKey {x:Type properties:Resources},
                                                                                                    AppShadeBrush}}"
                                                       RenderOptions.EdgeMode="Aliased"
                                                       StrokeThickness="0" />
                                            <StackPanel Grid.Row="0"
                                                        Margin="6,6,6,0"
                                                        Panel.ZIndex="250">
                                                <TextBlock Style="{DynamicResource {ComponentResourceKey {x:Type properties:Resources}, AppTitleTextStyle}}" Text="{Binding Type, Converter={StaticResource MailboxLocalizer}}" />
                                                <TextBlock Style="{DynamicResource {ComponentResourceKey {x:Type properties:Resources}, AppSubtitleTextStyle}}" Text="{x:Static properties:Resources.MailboxAssignmentDescription}" />
                                            </StackPanel>
                                            <StackPanel Grid.Row="1"
                                                        Margin="0,12,0,6"
                                                        Background="{DynamicResource {ComponentResourceKey {x:Type properties:Resources},
                                                                                                           AppWindowBackgroundBrush}}">
                                                <Border Padding="6">
                                                    <TreeView x:Name="CandidatesView"
                                                              MinHeight="200"
                                                              Background="{DynamicResource {ComponentResourceKey {x:Type properties:Resources},
                                                                                                                 AppWindowBackgroundBrush}}"
                                                              BorderThickness="0"
                                                              ItemsSource="{Binding MailboxCandidates}"
                                                              SelectedValuePath="Info">
                                                        <TreeView.ItemContainerStyle>
                                                            <!--  ReSharper disable Xaml.BindingWithContextNotResolved  -->
                                                            <Style TargetType="TreeViewItem">
                                                                <Setter Property="IsSelected" Value="{Binding IsSelected}" />
                                                                <Setter Property="IsExpanded" Value="{Binding IsExpanded}" />
                                                            </Style>
                                                            <!--  ReSharper restore Xaml.BindingWithContextNotResolved  -->
                                                        </TreeView.ItemContainerStyle>
                                                        <TreeView.ItemTemplate>
                                                            <HierarchicalDataTemplate DataType="paranoia:MailboxCandidateContext" ItemsSource="{Binding Children}">
                                                                <StackPanel Orientation="Horizontal">
                                                                    <TextBlock VerticalAlignment="Center"
                                                                               Style="{DynamicResource {ComponentResourceKey {x:Type properties:Resources},
                                                                                                                             AppNormalTextStyle}}"
                                                                               Text="{Binding Name}">
                                                                        <TextBlock.Foreground>
                                                                            <Binding Path="IsSelectable">
                                                                                <Binding.Converter>
                                                                                    <converters:BooleanToBrushConverter BrushForFalse="{DynamicResource {ComponentResourceKey {x:Type properties:Resources}, AppDisabledTextBrush}}" BrushForTrue="{DynamicResource {ComponentResourceKey {x:Type properties:Resources}, AppLightTextBrush}}" />
                                                                                </Binding.Converter>
                                                                            </Binding>
                                                                        </TextBlock.Foreground>
                                                                    </TextBlock>
                                                                </StackPanel>
                                                            </HierarchicalDataTemplate>
                                                        </TreeView.ItemTemplate>
                                                    </TreeView>
                                                </Border>

                                            </StackPanel>
                                            <Button Grid.Row="2"
                                                    Margin="6,0,6,6"
                                                    HorizontalAlignment="Right"
                                                    Command="{Binding AssignmentCommand}"
                                                    CommandParameter="{Binding SelectedValue,
                                                                               ElementName=CandidatesView}"
                                                    Content="Assign"
                                                    Padding="3" />
                                        </Grid>
                                    </Border>
                                </StackPanel>
                            </Popup>
                        </Grid>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>
        </Grid>
        <Grid Grid.Row="3"
              Grid.Column="2"
              Margin="0">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>
            <Rectangle Grid.Row="0"
                       Grid.RowSpan="2"
                       HorizontalAlignment="Stretch"
                       VerticalAlignment="Stretch"
                       Fill="{DynamicResource {ComponentResourceKey {x:Type properties:Resources},
                                                                    AppShadeBrush}}"
                       Opacity="{DynamicResource {ComponentResourceKey {x:Type properties:Resources},
                                                                       AppShadeOpacity}}" />
            <Border Grid.Row="0" Margin="16,0,0,0">
                <TextBlock VerticalAlignment="Center"
                           FontSize="{DynamicResource {ComponentResourceKey {x:Type properties:Resources},
                                                                            AppExtraLargeFontSize}}"
                           Style="{DynamicResource {ComponentResourceKey {x:Type properties:Resources},
                                                                         AppTitleTextStyle}}">
                    <Run Text="{x:Static properties:Resources.MessagesTitle}" />
                    <Run Foreground="{DynamicResource {ComponentResourceKey {x:Type properties:Resources}, AppMediumTextBrush}}" Text="{Binding Messages, Mode=OneWay, StringFormat={}({0}), Converter={StaticResource CollectionCounter}}" />
                </TextBlock>
            </Border>
            <ListView x:Name="MessagesListView"
                      Grid.Row="1"
                      HorizontalContentAlignment="Stretch"
                      Background="Transparent"
                      BorderThickness="0"
                      ItemsSource="{Binding Source={StaticResource MessagesSource}}"
                      KeyboardNavigation.DirectionalNavigation="Contained"
                      KeyboardNavigation.TabNavigation="None"
                      Padding="0"
                      SelectionChanged="OnMessageSelectionChanged"
                      TabIndex="4"
                      VirtualizingPanel.IsVirtualizing="True"
                      VirtualizingPanel.VirtualizationMode="Recycling">
                <ListView.Resources>
                    <converters:BooleanToBrushConverter x:Key="SeenBrushConverter"
                                                        BrushForFalse="{DynamicResource {ComponentResourceKey {x:Type properties:Resources},
                                                                                                              AppAccentBrush}}"
                                                        BrushForTrue="{DynamicResource {ComponentResourceKey {x:Type properties:Resources},
                                                                                                             AppMediumTextBrush}}" />
                    <converters:BooleanToOpacityConverter x:Key="SeenOpacityConverter"
                                                          OpacityForFalse="1.0"
                                                          OpacityForTrue="0.3" />
                </ListView.Resources>
                <ListView.InputBindings>
                    <KeyBinding Key="R"
                                Command="{Binding ReplyCommand}"
                                Modifiers="Control" />
                    <KeyBinding Key="F"
                                Command="{Binding ForwardCommand}"
                                Modifiers="Control" />
                    <KeyBinding Key="Delete" Command="{Binding DeleteMessageCommand}" />
                    <KeyBinding Key="D"
                                Command="{Binding DeleteMessageCommand}"
                                Modifiers="Control" />
                </ListView.InputBindings>
                <ListView.ContextMenu>
                    <ContextMenu>
                        <MenuItem Command="{Binding ReplyCommand}" InputGestureText="Ctrl + R">
                            <MenuItem.Header>
                                <TextBlock Style="{StaticResource AppMenuItemHeaderStyle}" Text="{x:Static properties:Resources.Reply}" />
                            </MenuItem.Header>
                            <!--
                                <MenuItem.Icon>
                                <Image Source="/Assets/reply.small.png" Style="{StaticResource AppIconImageStyle}" />
                                </MenuItem.Icon>
                            -->
                        </MenuItem>
                        <MenuItem Command="{Binding ForwardCommand}" InputGestureText="Ctrl + F">
                            <MenuItem.Header>
                                <TextBlock Style="{StaticResource AppMenuItemHeaderStyle}" Text="{x:Static properties:Resources.Forward}" />
                            </MenuItem.Header>
                            <!--
                                <MenuItem.Icon>
                                <Image Source="/Assets/forward.small.png" Style="{StaticResource AppIconImageStyle}" />
                                </MenuItem.Icon>
                            -->
                        </MenuItem>
                        <MenuItem Command="{Binding PrintCommand}" InputGestureText="Ctrl + P">
                            <MenuItem.Header>
                                <TextBlock Style="{StaticResource AppMenuItemHeaderStyle}" Text="{x:Static properties:Resources.Print}" />
                            </MenuItem.Header>
                            <!--
                                <MenuItem.Icon>
                                <Image Source="/Assets/print.small.png" Style="{StaticResource AppIconImageStyle}" />
                                </MenuItem.Icon>
                            -->
                        </MenuItem>
                        <MenuItem Command="{Binding DeleteMessageCommand}" InputGestureText="Entf">
                            <MenuItem.Header>
                                <TextBlock Style="{StaticResource AppMenuItemHeaderStyle}" Text="{x:Static properties:Resources.Delete}" />
                            </MenuItem.Header>
                            <!--
                                <MenuItem.Icon>
                                <Image Source="/Assets/delete.small.png" Style="{StaticResource AppIconImageStyle}" />
                                </MenuItem.Icon>
                            -->
                        </MenuItem>
                        <MenuItem Command="{Binding MarkAsSeenCommand}">
                            <MenuItem.Header>
                                <TextBlock Style="{StaticResource AppMenuItemHeaderStyle}" Text="{x:Static properties:Resources.MarkAsSeen}" />
                            </MenuItem.Header>
                            <!--
                                <MenuItem.Icon>
                                <Image Source="/Assets/delete.small.png" Style="{StaticResource AppIconImageStyle}" />
                                </MenuItem.Icon>
                            -->
                        </MenuItem>
                        <MenuItem Command="{Binding MarkAsNotSeenCommand}">
                            <MenuItem.Header>
                                <TextBlock Style="{StaticResource AppMenuItemHeaderStyle}" Text="{x:Static properties:Resources.MarkAsNotSeen}" />
                            </MenuItem.Header>
                            <!--
                                <MenuItem.Icon>
                                <Image Source="/Assets/delete.small.png" Style="{StaticResource AppIconImageStyle}" />
                                </MenuItem.Icon>
                            -->
                        </MenuItem>
                    </ContextMenu>
                </ListView.ContextMenu>
                <ListView.ItemsPanel>
                    <ItemsPanelTemplate>
                        <VirtualizingStackPanel x:Name="Mvsp" />
                    </ItemsPanelTemplate>
                </ListView.ItemsPanel>
                <ListView.ItemContainerStyle>
                    <Style BasedOn="{StaticResource {x:Type ListViewItem}}" TargetType="ListViewItem">
                        <!--  ReSharper disable once Xaml.BindingWithContextNotResolved  -->
                        <Setter Property="IsSelected" Value="{Binding IsSelected, Mode=TwoWay}" />
                        <Setter Property="Width" Value="{Binding ActualWidth, ElementName=Mvsp}" />
                        <Setter Property="Padding" Value="6,2,6,2" />
                    </Style>
                </ListView.ItemContainerStyle>
                <ListView.ItemTemplate>
                    <DataTemplate DataType="paranoia:MailMessageContext">
                        <Grid>
                            <Grid.RowDefinitions>
                                <RowDefinition />
                                <RowDefinition />
                            </Grid.RowDefinitions>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto" />
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="Auto" />
                            </Grid.ColumnDefinitions>
                            <Rectangle Grid.Row="0"
                                       Grid.RowSpan="2"
                                       Grid.Column="0"
                                       Grid.ColumnSpan="3" />
                            <Image Grid.Row="0"
                                   Grid.RowSpan="2"
                                   Grid.Column="0"
                                   Height="20"
                                   Margin="6,0,12,0"
                                   HorizontalAlignment="Center"
                                   VerticalAlignment="Center"
                                   Opacity="{Binding IsSeen,
                                                     Converter={StaticResource SeenOpacityConverter}}">
                                <Image.Source>
                                    <Binding Path="IsSeen">
                                        <Binding.Converter>
                                            <converters:BooleanToStringConverter StringForFalse="/Assets/unseen.png" StringForTrue="/Assets/seen.png" />
                                        </Binding.Converter>
                                    </Binding>
                                </Image.Source>
                            </Image>

                            <TextBlock Grid.Row="0"
                                       Grid.Column="1"
                                       Grid.ColumnSpan="2"
                                       VerticalAlignment="Center"
                                       FontSize="{DynamicResource {ComponentResourceKey {x:Type properties:Resources},
                                                                                        AppMediumLargeFontSize}}"
                                       FontWeight="Thin"
                                       Foreground="{Binding IsSubjectNilOrEmpty,
                                                            Converter={StaticResource SubjectBrushConverter}}"
                                       RenderOptions.ClearTypeHint="Enabled"
                                       Style="{DynamicResource {ComponentResourceKey {x:Type properties:Resources},
                                                                                     AppLightTextStyle}}"
                                       Text="{Binding Subject,
                                                      Converter={StaticResource SubjectFormatter}}"
                                       TextTrimming="CharacterEllipsis"
                                       TextWrapping="NoWrap" />

                            <TextBlock x:Name="FromBlock"
                                       Grid.Row="1"
                                       Grid.Column="1"
                                       HorizontalAlignment="Left"
                                       VerticalAlignment="Center"
                                       FontSize="{DynamicResource {ComponentResourceKey {x:Type properties:Resources},
                                                                                        AppNormalFontSize}}"
                                       Foreground="{Binding IsSeen,
                                                            Converter={StaticResource SeenBrushConverter}}"
                                       Text="{Binding Converter={StaticResource FromSenderFormatter}}" />

                            <TextBlock x:Name="DateBlock"
                                       Grid.Row="1"
                                       Grid.Column="2"
                                       HorizontalAlignment="Right"
                                       VerticalAlignment="Center"
                                       FontSize="{DynamicResource {ComponentResourceKey {x:Type properties:Resources},
                                                                                        AppSmallFontSize}}"
                                       Foreground="{DynamicResource {ComponentResourceKey {x:Type properties:Resources},
                                                                                          AppMediumTextBrush}}"
                                       Text="{Binding EntryDate,
                                                      Converter={StaticResource DateFormatter}}" />

                        </Grid>
                        <DataTemplate.Triggers>
                            <DataTrigger Binding="{Binding IsSelected}" Value="True">
                                <Setter TargetName="FromBlock" Property="Foreground" Value="{DynamicResource {ComponentResourceKey {x:Type properties:Resources}, AppLightTextBrush}}" />
                                <Setter TargetName="DateBlock" Property="Foreground" Value="{DynamicResource {ComponentResourceKey {x:Type properties:Resources}, AppLightTextBrush}}" />
                            </DataTrigger>
                        </DataTemplate.Triggers>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>
        </Grid>
        <GridSplitter Grid.Row="1"
                      Grid.RowSpan="3"
                      Grid.Column="3" />
        <Grid Grid.Row="3"
              Grid.Column="4"
              Grid.ColumnSpan="2"
              Margin="0">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>
            <Rectangle Grid.RowSpan="2"
                       HorizontalAlignment="Stretch"
                       VerticalAlignment="Stretch"
                       Fill="{DynamicResource {ComponentResourceKey {x:Type properties:Resources},
                                                                    AppShadeBrush}}"
                       Opacity="{DynamicResource {ComponentResourceKey {x:Type properties:Resources},
                                                                       AppShadeOpacity}}" />
            <Border Grid.Row="0" Padding="24,24,24,0">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition />
                        <RowDefinition />
                        <RowDefinition />
                    </Grid.RowDefinitions>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition />
                        <ColumnDefinition />
                    </Grid.ColumnDefinitions>
                    <TextBlock Grid.Row="0"
                               Grid.Column="0"
                               Grid.ColumnSpan="2"
                               Margin="0"
                               DataContext="{Binding SelectedMessage}"
                               Style="{DynamicResource {ComponentResourceKey {x:Type properties:Resources},
                                                                             AppTitleTextStyle}}"
                               Text="{Binding Subject,
                                              Mode=OneWay,
                                              Converter={StaticResource SubjectFormatter}}"
                               TextTrimming="CharacterEllipsis"
                               TextWrapping="NoWrap" />
                    <TextBlock Grid.Row="1"
                               Grid.Column="0"
                               DataContext="{Binding SelectedMessage}"
                               Style="{DynamicResource {ComponentResourceKey {x:Type properties:Resources},
                                                                             AppSubtitleTextStyle}}"
                               Text="{Binding FromName}" />
                    <TextBlock Grid.Row="2"
                               Grid.Column="0"
                               DataContext="{Binding SelectedMessage}"
                               FontSize="{DynamicResource {ComponentResourceKey {x:Type properties:Resources},
                                                                                AppSmallFontSize}}"
                               Style="{DynamicResource {ComponentResourceKey {x:Type properties:Resources},
                                                                             AppSubtitleTextStyle}}"
                               Text="{Binding EntryDate}" />
                    <StackPanel Grid.Row="1"
                                Grid.RowSpan="2"
                                Grid.Column="1"
                                HorizontalAlignment="Right"
                                VerticalAlignment="Center"
                                Orientation="Horizontal"
                                Visibility="{Binding SelectedMessage,
                                                     Delay=100,
                                                     Converter={StaticResource NullToVisibilityConverter}}">
                        <ui:MetroCircleButton Command="{Binding ReplyCommand}">
                            <Image Width="18"
                                   Margin="-2,2,0,0"
                                   Source="/Assets/reply.png" />
                        </ui:MetroCircleButton>
                        <ui:MetroCircleButton Command="{Binding ForwardCommand}">
                            <Image Width="18"
                                   Margin="-2,2,0,0"
                                   Source="/Assets/forward.png" />
                        </ui:MetroCircleButton>
                        <ui:MetroCircleButton Command="{Binding DeleteMessageCommand}">
                            <Image Width="18"
                                   Margin="0,0,0,0"
                                   Source="/Assets/delete.png" />
                        </ui:MetroCircleButton>
                    </StackPanel>
                </Grid>
            </Border>
            <Grid Grid.Row="1"
                  Margin="24,12,24,24"
                  HorizontalAlignment="Stretch"
                  VerticalAlignment="Stretch">
                <Border BorderBrush="{DynamicResource {ComponentResourceKey {x:Type properties:Resources},
                                                                            AppAccentBrush}}"
                        BorderThickness="0"
                        DataContext="{Binding SelectedMessage}"
                        Visibility="{Binding IsNotLoading,
                                             Converter={StaticResource BooleanToVisibilityConverter}}">
                    <Border Visibility="{Binding Converter={StaticResource NullToVisibilityConverter}}">
                        <ui:HtmlControl x:Name="HtmlControl"
                                        Background="Transparent"
                                        BorderThickness="0"
                                        DataContext="{Binding DataContext,
                                                              ElementName=Window}"
                                        IsReadOnly="True"
                                        IsTransparent="False"
                                        Source="{Binding Source,
                                                         Mode=OneWay}"
                                        Zoom="{Binding Zoom,
                                                       Mode=TwoWay}"
                                        d:IsHidden="True" />
                    </Border>
                </Border>
                <Grid DataContext="{Binding SelectedMessage}" Visibility="{Binding IsLoading, Converter={StaticResource BooleanToVisibilityConverter}, FallbackValue=Collapsed}">
                    <StackPanel VerticalAlignment="Center">
                        <TextBlock Margin="6"
                                   HorizontalAlignment="Center"
                                   FontSize="{DynamicResource {ComponentResourceKey {x:Type properties:Resources},
                                                                                    AppNormalFontSize}}"
                                   FontWeight="Thin"
                                   Foreground="{DynamicResource {ComponentResourceKey {x:Type properties:Resources},
                                                                                      AppLightTextBrush}}"
                                   Text="{x:Static properties:Resources.MessageDownload}" />
                        <ProgressBar Height="4"
                                     Margin="12"
                                     HorizontalAlignment="Stretch"
                                     Maximum="{Binding Size}"
                                     Minimum="0"
                                     Value="{Binding BytesReceived}" />
                    </StackPanel>
                </Grid>
            </Grid>
        </Grid>
        <Grid x:Name="Overlay"
              Grid.Row="0"
              Grid.RowSpan="4"
              Grid.Column="0"
              Grid.ColumnSpan="6"
              Panel.ZIndex="1000"
              RenderTransformOrigin="0.5,0.5"
              Visibility="Collapsed">
            <Grid.RenderTransform>
                <TransformGroup>
                    <ScaleTransform />
                    <SkewTransform />
                    <RotateTransform />
                    <TranslateTransform />
                </TransformGroup>
            </Grid.RenderTransform>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="200" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>
            <Grid Grid.ColumnSpan="2"
                  Background="{DynamicResource {ComponentResourceKey {x:Type properties:Resources},
                                                                     AppShadeBrush}}"
                  Opacity="{DynamicResource {ComponentResourceKey {x:Type properties:Resources},
                                                                  AppShadeOpacity}}" />
            <Border x:Name="OverlayContent"
                    Grid.Column="1"
                    Background="{DynamicResource {ComponentResourceKey {x:Type properties:Resources},
                                                                       AppOverlayBackroundBrush}}"
                    Padding="24">
                <Border.RenderTransform>
                    <TransformGroup>
                        <ScaleTransform />
                        <SkewTransform />
                        <RotateTransform />
                        <TranslateTransform />
                    </TransformGroup>
                </Border.RenderTransform>
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="*" />
                    </Grid.RowDefinitions>
                    <StackPanel x:Name="StackPanel"
                                Grid.Row="0"
                                Margin="0,0,0,12"
                                Orientation="Horizontal">
                        <ui:MetroCircleButton x:Name="Button"
                                              Margin="0,0,12,0"
                                              Command="ui:WindowCommands.CloseFlyOut"
                                              Focusable="False"
                                              RenderTransformOrigin="0.5,0.5">
                            <Button.RenderTransform>
                                <TransformGroup>
                                    <ScaleTransform />
                                    <SkewTransform />
                                    <RotateTransform />
                                    <TranslateTransform />
                                </TransformGroup>
                            </Button.RenderTransform>
                            <Image Width="18"
                                   HorizontalAlignment="Center"
                                   Source="/Assets/back.png" />
                        </ui:MetroCircleButton>
                        <TextBlock x:Name="TextBlock"
                                   Margin="0"
                                   VerticalAlignment="Center"
                                   DataContext="{Binding Content,
                                                         ElementName=FlyOutFrame}"
                                   Padding="0,0,0,2"
                                   RenderTransformOrigin="0.5,0.5"
                                   Style="{DynamicResource {ComponentResourceKey {x:Type properties:Resources},
                                                                                 AppTitleTextStyle}}"
                                   Text="{Binding Title}"
                                   d:DataContext="{d:DesignInstance Page}">
                            <TextBlock.RenderTransform>
                                <TransformGroup>
                                    <ScaleTransform />
                                    <SkewTransform />
                                    <RotateTransform />
                                    <TranslateTransform />
                                </TransformGroup>
                            </TextBlock.RenderTransform>
                        </TextBlock>
                    </StackPanel>
                    <Frame x:Name="FlyOutFrame"
                           Grid.Row="1"
                           HorizontalAlignment="Stretch"
                           VerticalAlignment="Stretch"
                           KeyboardNavigation.TabNavigation="Cycle"
                           Navigated="OnFlyOutFrameNavigated"
                           NavigationUIVisibility="Hidden"
                           RenderTransformOrigin="0.5,0.5">
                        <Frame.RenderTransform>
                            <TransformGroup>
                                <ScaleTransform />
                                <SkewTransform />
                                <RotateTransform />
                                <TranslateTransform />
                            </TransformGroup>
                        </Frame.RenderTransform>
                    </Frame>
                </Grid>
            </Border>
        </Grid>
        <Border Grid.Row="1"
                Grid.Column="0"
                Grid.ColumnSpan="6"
                Margin="0,-1,0,0"
                VerticalAlignment="Top"
                BorderBrush="{DynamicResource {ComponentResourceKey {x:Type properties:Resources},
                                                                    AppAccentBrush}}"
                BorderThickness="0,1,0,0" />
        <StatusBar Grid.Row="4"
                   Grid.Column="0"
                   Grid.ColumnSpan="6"
                   Margin="0"
                   Background="{StaticResource AppAccentBrush}"
                   Padding="0">
            <StatusBar.ItemsPanel>
                <ItemsPanelTemplate>
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto" />
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="Auto" />
                            <ColumnDefinition Width="Auto" />
                            <ColumnDefinition Width="24" />
                        </Grid.ColumnDefinitions>
                    </Grid>
                </ItemsPanelTemplate>
            </StatusBar.ItemsPanel>
            <StatusBarItem Grid.Column="0">
                <TextBlock Text="{Binding StatusText, Mode=OneWay}" />
            </StatusBarItem>
            <StatusBarItem Grid.Column="2">
                <Slider Width="150"
                        IsEnabled="{Binding IsMessageSelected}"
                        IsTabStop="False"
                        LargeChange="25.0"
                        Maximum="400.0"
                        Minimum="1.0"
                        SmallChange="5.0"
                        TickPlacement="None"
                        Value="{Binding Zoom,
                                        Mode=TwoWay}" />
            </StatusBarItem>
            <StatusBarItem Grid.Column="3" Padding="0">
                <Button Width="50"
                        Command="{Binding ResetZoomCommand}"
                        Content="{Binding Zoom,
                                          Mode=OneWay,
                                          Converter={StaticResource PercentageFormatter}}"
                        IsEnabled="{Binding IsMessageSelected}"
                        IsTabStop="False"
                        Style="{DynamicResource {ComponentResourceKey {x:Type properties:Resources},
                                                                      StatusBarButtonStyle}}" />
            </StatusBarItem>
        </StatusBar>
        <Grid Grid.Row="0"
              Grid.RowSpan="5"
              Grid.Column="0"
              Grid.ColumnSpan="6"
              Visibility="{Binding IsPopupVisible,
                                   Converter={StaticResource BooleanToVisibilityConverter}}"
              ZIndex="100000">
            <Grid.RowDefinitions>
                <RowDefinition Height="*" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>
            <Rectangle Grid.Row="0"
                       Fill="{DynamicResource {ComponentResourceKey {x:Type properties:Resources},
                                                                    AppShadeBrush}}"
                       Opacity="{DynamicResource {ComponentResourceKey {x:Type properties:Resources},
                                                                       AppShadeOpacity}}"
                       RenderOptions.EdgeMode="Aliased" />
            <Rectangle Grid.Row="2"
                       Fill="{DynamicResource {ComponentResourceKey {x:Type properties:Resources},
                                                                    AppShadeBrush}}"
                       Opacity="{DynamicResource {ComponentResourceKey {x:Type properties:Resources},
                                                                       AppShadeOpacity}}"
                       RenderOptions.EdgeMode="Aliased" />
            <Border Grid.Row="1"
                    Background="{DynamicResource {ComponentResourceKey {x:Type properties:Resources},
                                                                       AppOverlayBackroundBrush}}"
                    BorderBrush="{DynamicResource {ComponentResourceKey {x:Type properties:Resources},
                                                                        AppAccentBrush}}"
                    BorderThickness="0"
                    Padding="0,24,0,24">
                <Grid Width="600" HorizontalAlignment="Center">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="*" />
                    </Grid.RowDefinitions>
                    <TextBlock Margin="0,0,0,24"
                               VerticalAlignment="Center"
                               DataContext="{Binding Content,
                                                     ElementName=PopupFrame}"
                               Padding="0,0,0,2"
                               Style="{DynamicResource {ComponentResourceKey {x:Type properties:Resources},
                                                                             AppTitleTextStyle}}"
                               Text="{Binding Title}"
                               d:DataContext="{d:DesignInstance Page}" />
                    <Frame x:Name="PopupFrame"
                           Grid.Row="1"
                           MinHeight="200"
                           NavigationUIVisibility="Hidden" />
                </Grid>

            </Border>

        </Grid>
    </Grid>
</ui:MetroWindow>