<ui:MetroWindow x:Class="Crystalbyte.Paranoia.MainWindow"
                xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                xmlns:converters="clr-namespace:Crystalbyte.Paranoia.UI.Converters"
                xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
                xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
                xmlns:paranoia="clr-namespace:Crystalbyte.Paranoia"
                xmlns:ui="clr-namespace:Crystalbyte.Paranoia.UI"
                Title="p4r4n014"
                Width="1080"
                Height="668"
                MinWidth="800"
                MinHeight="600"
                Background="{StaticResource WindowBackgroundBrush}"
                Icon="/Assets/icon.png"
                RenderOptions.ClearTypeHint="Enabled"
                UseLayoutRounding="True"
                WindowStartupLocation="CenterScreen"
                d:DataContext="{d:DesignInstance paranoia:AppContext}"
                mc:Ignorable="d">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="250" />
            <ColumnDefinition Width="80" />
            <ColumnDefinition Width="Auto"
                              MinWidth="250"
                              MaxWidth="400" />
            <ColumnDefinition Width="Auto" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>
        <Grid Grid.Row="0"
              Grid.Column="0"
              Grid.ColumnSpan="2"
              Margin="16,10,16,0"
              DataContext="{Binding SelectedAccount,
                                    RelativeSource={RelativeSource FindAncestor,
                                                                   AncestorType=ui:MetroWindow}}"
              d:DataContext="{d:DesignInstance paranoia:MailAccountContext}">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>
            <Image Grid.Row="0"
                   Grid.Column="0"
                   Width="64"
                   Margin="0,0,16,16"
                   HorizontalAlignment="Left"
                   Source="{Binding Address,
                                    Converter={StaticResource GravatarImageConverter}}" />
            <StackPanel Grid.Column="1"
                        HorizontalAlignment="Left"
                        VerticalAlignment="Top">
                <TextBlock Style="{StaticResource AppTitleTextStyle}"
                           Text="{Binding Name}"
                           TextTrimming="CharacterEllipsis"
                           TextWrapping="NoWrap" />
                <TextBlock Style="{StaticResource AppSubtitleTextStyle}" Text="{Binding Address}" />
            </StackPanel>

        </Grid>
        <Grid Grid.Row="1"
              Grid.Column="0"
              Background="#333"
              DataContext="{Binding SelectedAccount,
                                    RelativeSource={RelativeSource FindAncestor,
                                                                   AncestorType=ui:MetroWindow}}"
              d:DataContext="{d:DesignInstance paranoia:MailAccountContext}">
            <ListView Background="{StaticResource WindowLightBackgroundBrush}"
                      BorderThickness="0"
                      ItemsSource="{Binding Contacts}"
                      SelectionChanged="OnContactsSelectionChanged">
                <ListView.ItemContainerStyle>
                    <Style TargetType="ListViewItem">
                        <Setter Property="Margin" Value="0" />
                        <Setter Property="Padding" Value="6,3,6,3" />
                        <Setter Property="IsSelected" Value="{Binding IsSelected, Mode=TwoWay}" />
                    </Style>
                </ListView.ItemContainerStyle>
                <ListView.ItemTemplate>
                    <DataTemplate>
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto" />
                                <ColumnDefinition Width="*" />
                            </Grid.ColumnDefinitions>
                            <Image Grid.Row="0"
                                   Grid.Column="0"
                                   Width="38"
                                   Margin="0,0,6,0"
                                   Source="{Binding Address,
                                                    Converter={StaticResource GravatarImageConverter}}" />
                            <StackPanel Grid.Column="1"
                                        Margin="0,-3,0,0"
                                        VerticalAlignment="Center">
                                <TextBlock FontSize="18"
                                           Style="{StaticResource AppTitleTextStyle}"
                                           Text="{Binding Name}" />
                                <TextBlock FontSize="{StaticResource AppSmallFontSize}"
                                           Style="{StaticResource AppSubtitleTextStyle}"
                                           Text="{Binding Address}" />
                            </StackPanel>
                        </Grid>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>
        </Grid>
        <Grid Grid.Row="1" Grid.Column="1">
            <ListView Background="{StaticResource WindowLightBackgroundBrush}"
                      BorderThickness="0"
                      DataContext="{Binding SelectedAccount,
                                            RelativeSource={RelativeSource FindAncestor,
                                                                           AncestorType=ui:MetroWindow}}"
                      ItemsSource="{Binding Mailboxes}"
                      SelectionChanged="OnMailboxSelectionChanged"
                      d:DataContext="{d:DesignInstance paranoia:MailAccountContext}">
                <ListView.ItemContainerStyle>
                    <Style TargetType="ListViewItem">
                        <Setter Property="Margin" Value="0" />
                        <Setter Property="Padding" Value="2" />
                        <Setter Property="IsSelected" Value="{Binding IsSelected, Mode=TwoWay}" />
                    </Style>
                </ListView.ItemContainerStyle>
                <ListView.ItemsPanel>
                    <ItemsPanelTemplate>
                        <StackPanel Orientation="Vertical" />
                    </ItemsPanelTemplate>
                </ListView.ItemsPanel>
                <ListView.ItemTemplate>
                    <DataTemplate>
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto" />
                                <ColumnDefinition Width="*" />
                            </Grid.ColumnDefinitions>
                            <Image Grid.Row="0"
                                   Grid.Column="0"
                                   Width="24"
                                   HorizontalAlignment="Left"
                                   Source="/Assets/folder.png"
                                   ToolTip="{Binding Name}" />
                            <TextBlock Grid.Column="1"
                                       VerticalAlignment="Center"
                                       FontSize="{StaticResource AppSmallFontSize}"
                                       Style="{StaticResource AppNormalTextStyle}"
                                       Text="{Binding Type,
                                                      Converter={StaticResource MailboxLocalizer}}">
                                <TextBlock.Foreground>
                                    <Binding Path="IsAssigned">
                                        <Binding.Converter>
                                            <converters:BooleanToBrushConverter BrushForFalse="{StaticResource AppControlBrush}" BrushForTrue="{StaticResource AppLightTextBrush}" />
                                        </Binding.Converter>
                                    </Binding>
                                </TextBlock.Foreground>
                            </TextBlock>
                        </Grid>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>
        </Grid>
        <Grid Grid.Row="0"
              Grid.RowSpan="2"
              Grid.Column="2">
            <ListView Background="{StaticResource WindowLightBackgroundBrush}"
                      BorderThickness="0"
                      ItemsSource="{Binding MessagesSource}">
                <ListView.ItemContainerStyle>
                    <Style TargetType="ListViewItem" d:DataContext="{d:DesignInstance paranoia:MailMessageContext}">
                        <Setter Property="Margin" Value="0" />
                        <Setter Property="Padding" Value="2" />
                        <Setter Property="IsSelected" Value="{Binding IsSelected, Mode=TwoWay}" />
                        <Setter Property="ToolTip" Value="{Binding Subject}" />
                    </Style>
                </ListView.ItemContainerStyle>
                <ListView.ItemsPanel>
                    <ItemsPanelTemplate>
                        <StackPanel Orientation="Vertical" />
                    </ItemsPanelTemplate>
                </ListView.ItemsPanel>
                <ListView.ItemTemplate>
                    <DataTemplate DataType="paranoia:MailMessageContext">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto" />
                                <ColumnDefinition Width="*" />
                            </Grid.ColumnDefinitions>
                            <Image Grid.Row="0"
                                   Grid.Column="0"
                                   Width="24"
                                   HorizontalAlignment="Left"
                                   VerticalAlignment="Center"
                                   Source="{Binding FromAddress,
                                                    IsAsync=True,
                                                    Converter={StaticResource GravatarImageConverter}}" />
                            <StackPanel Grid.Column="1" VerticalAlignment="Center">
                                <TextBlock HorizontalAlignment="Left"
                                           Style="{StaticResource AppNormalTextStyle}"
                                           Text="{Binding Subject}"
                                           TextTrimming="CharacterEllipsis"
                                           TextWrapping="NoWrap">
                                    <!--
                                        <TextBlock.Foreground>
                                        <Binding Path="IsAssigned">
                                        <Binding.Converter>
                                        <converters:BooleanToBrushConverter BrushForFalse="{StaticResource AppControlBrush}" BrushForTrue="{StaticResource AppLightTextBrush}" />
                                        </Binding.Converter>
                                        </Binding>
                                        </TextBlock.Foreground>
                                    -->
                                </TextBlock>
                                <TextBlock HorizontalAlignment="Left"
                                           FontSize="{StaticResource AppSmallFontSize}"
                                           Style="{StaticResource AppSubtitleTextStyle}"
                                           Text="{Binding FromName}" />
                            </StackPanel>

                        </Grid>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>
        </Grid>
        <GridSplitter Grid.Row="0"
                      Grid.RowSpan="2"
                      Grid.Column="3"
                      Width="4"
                      HorizontalAlignment="Center"
                      VerticalAlignment="Stretch" />
        <Grid Grid.Row="0"
              Grid.RowSpan="2"
              Grid.Column="4"
              Background="White" />

    </Grid>
</ui:MetroWindow>