<Page x:Class="Crystalbyte.Paranoia.UI.CreateAccountStartFlyoutPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:commands="clr-namespace:Crystalbyte.Paranoia.UI.Commands"
      xmlns:controls="clr-namespace:Crystalbyte.Paranoia.UI;assembly=Crystalbyte.Paranoia.Controls"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      xmlns:paranoia="clr-namespace:Crystalbyte.Paranoia"
      xmlns:properties="clr-namespace:Crystalbyte.Paranoia.Properties"
      xmlns:themes="clr-namespace:Crystalbyte.Paranoia.Themes;assembly=Crystalbyte.Paranoia.Themes"
      xmlns:ui="clr-namespace:Crystalbyte.Paranoia.UI"
      Title="{x:Static properties:Resources.CreateAccountTitle}"
      Background="{DynamicResource {x:Static themes:ThemeResourceKeys.FlyoutBackgroundBrushKey}}"
      d:DataContext="{d:DesignInstance paranoia:MailAccountContext}"
      d:DesignHeight="300"
      d:DesignWidth="300"
      mc:Ignorable="d">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>
        <RadioButton x:Name="AutoMagicButton"
                     Grid.Row="0"
                     Grid.Column="0"
                     Grid.ColumnSpan="2"
                     Margin="0,12,0,12"
                     Checked="OnAutoMagicButtonCheckedChanged"
                     Foreground="{DynamicResource {x:Static themes:ThemeResourceKeys.FlyoutTextForegroundBrushKey}}"
                     GroupName="method"
                     IsChecked="{Binding IsAutoDetectPreferred,
                                         Mode=TwoWay}"
                     Style="{DynamicResource RadioButtonFlyoutStyle}"
                     Unchecked="OnAutoMagicButtonCheckedChanged">
            <TextBlock Foreground="{DynamicResource {x:Static themes:ThemeResourceKeys.FlyoutTextForegroundBrushKey}}" Text="{x:Static properties:Resources.DetectAutomagically}" />
        </RadioButton>
        <TextBlock Grid.Row="1"
                   Grid.Column="0"
                   Foreground="{DynamicResource {x:Static themes:ThemeResourceKeys.FlyoutTextForegroundBrushKey}}"
                   IsEnabled="{Binding IsChecked,
                                       ElementName=AutoMagicButton}"
                   Style="{StaticResource LabelTextStyle}">
            <Run Text="{x:Static properties:Resources.AccountName}" />
            <Run>:</Run>
        </TextBlock>
        <TextBox x:Name="NameTextBox"
                 Grid.Row="1"
                 Grid.Column="1"
                 Width="400"
                 HorizontalAlignment="Left"
                 Foreground="{DynamicResource {x:Static themes:ThemeResourceKeys.FlyoutTextForegroundBrushKey}}"
                 IsEnabled="{Binding IsChecked,
                                     ElementName=AutoMagicButton}"
                 Style="{DynamicResource TextBoxFlyoutStyle}"
                 Text="{Binding Name,
                                Mode=TwoWay,
                                UpdateSourceTrigger=PropertyChanged}"
                 TextChanged="OnNameTextChanged" />
        <TextBlock Grid.Row="2"
                   Grid.Column="0"
                   Foreground="{DynamicResource {x:Static themes:ThemeResourceKeys.FlyoutTextForegroundBrushKey}}"
                   IsEnabled="{Binding IsChecked,
                                       ElementName=AutoMagicButton}"
                   Style="{StaticResource LabelTextStyle}">
            <Run Text="{x:Static properties:Resources.MailAddress}" />
            <Run>:</Run>
        </TextBlock>
        <TextBox Grid.Row="2"
                 Grid.Column="1"
                 Width="400"
                 HorizontalAlignment="Left"
                 Foreground="{DynamicResource {x:Static themes:ThemeResourceKeys.FlyoutTextForegroundBrushKey}}"
                 IsEnabled="{Binding IsChecked,
                                     ElementName=AutoMagicButton}"
                 Style="{DynamicResource TextBoxFlyoutStyle}"
                 Text="{Binding Address,
                                Mode=TwoWay,
                                UpdateSourceTrigger=PropertyChanged}"
                 TextChanged="OnAddressTextChanged" />
        <TextBlock Grid.Row="3"
                   Grid.Column="0"
                   Foreground="{DynamicResource {x:Static themes:ThemeResourceKeys.FlyoutTextForegroundBrushKey}}"
                   IsEnabled="{Binding IsChecked,
                                       ElementName=AutoMagicButton}"
                   Style="{StaticResource LabelTextStyle}">
            <Run Text="{x:Static properties:Resources.Password}" />
            <Run>:</Run>
        </TextBlock>
        <PasswordBox x:Name="PasswordBox"
                     Grid.Row="3"
                     Grid.Column="1"
                     Width="400"
                     HorizontalAlignment="Left"
                     Foreground="{DynamicResource {x:Static themes:ThemeResourceKeys.FlyoutTextForegroundBrushKey}}"
                     IsEnabled="{Binding IsChecked,
                                         ElementName=AutoMagicButton}"
                     Style="{DynamicResource FlyoutPasswordBoxStyle}" />
        <RadioButton Grid.Row="4"
                     Grid.Column="0"
                     Grid.ColumnSpan="2"
                     Margin="0,12,0,12"
                     GroupName="method"
                     Style="{DynamicResource RadioButtonFlyoutStyle}">
            <TextBlock Foreground="{DynamicResource {x:Static themes:ThemeResourceKeys.FlyoutTextForegroundBrushKey}}" Text="{x:Static properties:Resources.EnterManually}" />
        </RadioButton>
        <StackPanel Grid.Row="5"
                    Grid.Column="0"
                    Grid.ColumnSpan="2"
                    Margin="0,12,0,0"
                    Visibility="{Binding IsDetectingSettings,
                                         Converter={StaticResource BooleanToVisibilityConverter}}">
            <controls:MetroProgressBar Height="4" IsIndeterminate="True" />
            <TextBlock Margin="0,6,0,0"
                       Foreground="{DynamicResource {x:Static themes:ThemeResourceKeys.FlyoutTextForegroundBrushKey}}"
                       Text="{x:Static properties:Resources.DetectSettings}" />
        </StackPanel>
        <StackPanel Grid.Row="6"
                    Grid.Column="0"
                    Grid.ColumnSpan="2"
                    Orientation="Horizontal">
            <controls:MetroButton x:Name="ContinueButton"
                                  Margin="12,0"
                                  Command="ui:FlyoutCommands.Continue"
                                  Content="{x:Static properties:Resources.Continue}"
                                  ImageSource="/Assets/ok.png"
                                  Style="{DynamicResource MetroButtonFlyoutStyle}" />
            <controls:MetroButton Margin="12,0"
                                  Command="ui:FlyoutCommands.Close"
                                  Content="{x:Static properties:Resources.Cancel}"
                                  ImageSource="/Assets/cancel.png"
                                  Style="{DynamicResource MetroButtonFlyoutStyle}" />
        </StackPanel>
    </Grid>
</Page>