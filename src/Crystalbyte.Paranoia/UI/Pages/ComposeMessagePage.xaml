<Page x:Class="Crystalbyte.Paranoia.UI.Pages.ComposeMessagePage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:CKE="clr-namespace:CKEditorDotNet;assembly=CKEditorDotNet"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      xmlns:paranoia="clr-namespace:Crystalbyte.Paranoia"
      xmlns:properties="clr-namespace:Crystalbyte.Paranoia.Properties"
      xmlns:system="clr-namespace:System;assembly=mscorlib"
      xmlns:ui="clr-namespace:Crystalbyte.Paranoia.UI"
      Title="{x:Static properties:Resources.ComposeMessagePageTitle}"
      FocusManager.FocusedElement="{Binding ElementName=RecipientsBox}"
      d:DataContext="{d:DesignInstance paranoia:MailCompositionContext}"
      d:DesignHeight="300"
      d:DesignWidth="300"
      mc:Ignorable="d">
    <Page.InputBindings>
        <KeyBinding Key="Enter"
                    Command="{Binding SendCommand}"
                    Modifiers="Control" />
    </Page.InputBindings>
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <TextBlock Grid.Row="0"
                   Grid.Column="0"
                   VerticalAlignment="Center"
                   Text="{x:Static properties:Resources.Recipients}" />
        <ui:AutoCompleteBox x:Name="RecipientsBox"
                            Grid.Row="0"
                            Grid.Column="1"
                            HorizontalAlignment="Stretch"
                            ItemsSource="{Binding Suggestions}"
                            ItemsSourceRequested="OnRecipientsBoxItemsSourceRequested"
                            SelectedValuesChanged="OnRecipientsBoxSelectionChanged">
            <ui:AutoCompleteBox.StringTokenTemplate>
                <DataTemplate DataType="system:String">
                    <Grid>
                        <Rectangle Fill="{DynamicResource {ComponentResourceKey {x:Type properties:Resources}, Metro.AccentBrush}}" RenderOptions.EdgeMode="Aliased" />
                        <TextBlock Margin="2,0,2,0"
                                   Background="Transparent"
                                   FontWeight="Regular"
                                   Text="{Binding}" />
                    </Grid>
                </DataTemplate>
            </ui:AutoCompleteBox.StringTokenTemplate>
            <ui:AutoCompleteBox.TokenTemplate>
                <DataTemplate DataType="paranoia:MailContactContext">
                    <Grid>
                        <Rectangle Fill="{DynamicResource {ComponentResourceKey {x:Type properties:Resources},
                                                                                Metro.AccentBrush}}"
                                   RenderOptions.EdgeMode="Aliased"
                                   SnapsToDevicePixels="True" />
                        <StackPanel Orientation="Horizontal">
                            <Image Width="16"
                                   VerticalAlignment="Center"
                                   RenderOptions.BitmapScalingMode="LowQuality"
                                   Source="{Binding Address,
                                                    IsAsync=True,
                                                    Converter={StaticResource GravatarImageConverter},
                                                    ConverterParameter=16}" />
                            <TextBlock Margin="2,0,2,0"
                                       Background="Transparent"
                                       FontWeight="Regular"
                                       Foreground="{DynamicResource {ComponentResourceKey {x:Type properties:Resources},
                                                                                          Metro.NormalTextBrush}}"
                                       Text="{Binding Name}" />
                        </StackPanel>

                    </Grid>
                </DataTemplate>
            </ui:AutoCompleteBox.TokenTemplate>
            <ui:AutoCompleteBox.ItemTemplate>
                <DataTemplate DataType="paranoia:MailContactContext">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto" />
                            <ColumnDefinition Width="*" />
                        </Grid.ColumnDefinitions>
                        <Image Grid.Column="0"
                               Width="40"
                               Margin="2,2,12,2"
                               HorizontalAlignment="Stretch"
                               RenderOptions.BitmapScalingMode="Fant"
                               Source="{Binding Address,
                                                IsAsync=True,
                                                Converter={StaticResource GravatarImageConverter}}"
                               Stretch="Uniform" />
                        <StackPanel Grid.Column="1" VerticalAlignment="Center">
                            <TextBlock FontSize="{DynamicResource {ComponentResourceKey {x:Type properties:Resources},
                                                                                        Metro.MediumLargeFontSize}}"
                                       FontWeight="Thin"
                                       Foreground="{DynamicResource {ComponentResourceKey {x:Type properties:Resources},
                                                                                          Metro.NormalTextBrush}}"
                                       Text="{Binding Name}" />
                            <TextBlock FontWeight="Thin"
                                       Foreground="{DynamicResource {ComponentResourceKey {x:Type properties:Resources},
                                                                                          Metro.SecondaryTextBrush}}"
                                       Text="{Binding Address}" />
                        </StackPanel>
                    </Grid>
                </DataTemplate>
            </ui:AutoCompleteBox.ItemTemplate>
        </ui:AutoCompleteBox>
        <TextBlock Grid.Row="1"
                   Grid.Column="0"
                   Margin="0,0,12,0"
                   VerticalAlignment="Center"
                   Text="{x:Static properties:Resources.Subject}" />
        <TextBox x:Name="SubjectTextBox"
                 Grid.Row="1"
                 Grid.Column="1"
                 Text="{Binding Subject,
                                Mode=TwoWay}" />
        <ListView ItemsSource="{Binding Attachments}" Grid.Row="2" Grid.ColumnSpan="2" Background="Transparent">
            <ListView.Style>
                <Style TargetType="ListView">
                    <Style.Triggers>
                        <Trigger Property="HasItems" Value="false">
                            <Setter Property="Visibility" Value="Hidden"/>
                        </Trigger>
                    </Style.Triggers>
                </Style>
            </ListView.Style>
            <ListView.ItemsPanel>
                <ItemsPanelTemplate>
                    <WrapPanel Orientation="Horizontal"/>
                </ItemsPanelTemplate>
            </ListView.ItemsPanel>
            <ListView.ItemTemplate>
                <ItemContainerTemplate>
                    <StackPanel Orientation="Horizontal" Background="Transparent" ToolTip="{Binding FullName}">
                        <TextBlock Text="{Binding Name}" Margin="2,0,2,0" Foreground="#BADA55"/>
                        <Button Content="X" Command="{Binding RemoveAttachmentCommand}"/>
                    </StackPanel>
                </ItemContainerTemplate>
            </ListView.ItemTemplate>
        </ListView>
        <ui:HtmlControl x:Name="HtmlControl"
            Grid.Row="3"
            Grid.Column="0"
            Grid.ColumnSpan="2"
            Margin="0,3,0,12"
            HorizontalAlignment="Stretch"
            VerticalAlignment="Stretch"
            Background="{DynamicResource {ComponentResourceKey {x:Type properties:Resources},
            Metro.ControlBackgroundBrush}}"
            IsTransparent="True"
            Padding="3"
            Source="{Binding Source,
            Mode=TwoWay}" />

        <ui:MetroCircleButton Grid.Row="4"
                              Grid.Column="0"
                              HorizontalAlignment="Left"
                              Command="{Binding SendCommand}"
                              Text="{x:Static properties:Resources.Send}">
            <Image Width="18"
                   Margin="0,0,0,0"
                   Source="/Assets/message.png" />
        </ui:MetroCircleButton>
        <ui:MetroCircleButton Grid.Row="4"
                              Grid.Column="1"
                              HorizontalAlignment="Right"
                              Command="{Binding AddattachmentCommand}"
                              Text="{x:Static properties:Resources.AddAttachment}">
            <Image Width="18"
                   Margin="0,0,0,0"
                   Source="/Assets/message.png" />
        </ui:MetroCircleButton>
    </Grid>
</Page>