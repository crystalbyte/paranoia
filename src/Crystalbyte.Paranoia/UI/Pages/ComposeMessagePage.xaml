<Page x:Class="Crystalbyte.Paranoia.UI.Pages.ComposeMessagePage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      xmlns:paranoia="clr-namespace:Crystalbyte.Paranoia"
      xmlns:properties="clr-namespace:Crystalbyte.Paranoia.Properties"
      xmlns:system="clr-namespace:System;assembly=mscorlib"
      xmlns:ui="clr-namespace:Crystalbyte.Paranoia.UI"
      Title="{x:Static properties:Resources.ComposeMessagePageTitle}"
      d:DataContext="{d:DesignInstance paranoia:MailCompositionContext}"
      d:DesignHeight="300"
      d:DesignWidth="300"
      mc:Ignorable="d">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition />
            <ColumnDefinition />
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <TextBox x:Name="SubjectTextBox"
                 Grid.Row="0"
                 Grid.Column="0"
                 Grid.ColumnSpan="2"
                 TabIndex="0"
                 Text="{Binding Subject,
                                Mode=TwoWay}" />
        <ui:AutoCompleteBox x:Name="SuggestionBox"
                            Grid.Row="1"
                            Grid.Column="0"
                            Grid.ColumnSpan="2"
                            HorizontalAlignment="Stretch"
                            ItemsSource="{Binding Suggestions}"
                            ItemsSourceRequested="OnAutoCompleteBoxOnItemsSourceRequested"
                            TabIndex="1">
            <ui:AutoCompleteBox.StringTokenTemplate>
                <DataTemplate DataType="system:String">
                    <Grid>
                        <Rectangle Fill="{DynamicResource {ComponentResourceKey {x:Type properties:Resources}, AppAccentBrush}}" RenderOptions.EdgeMode="Aliased" />
                        <TextBlock Margin="2,0,2,0"
                                   Background="Transparent"
                                   FontWeight="Regular"
                                   Text="{Binding}" />
                    </Grid>
                </DataTemplate>
            </ui:AutoCompleteBox.StringTokenTemplate>
            <ui:AutoCompleteBox.TokenTemplate>
                <DataTemplate DataType="paranoia:MailContactContext">
                    <Grid>
                        <Rectangle Fill="{DynamicResource {ComponentResourceKey {x:Type properties:Resources},
                                                                                AppAccentBrush}}"
                                   RenderOptions.EdgeMode="Aliased"
                                   SnapsToDevicePixels="True" />
                        <StackPanel Orientation="Horizontal">
                            <Image Width="16"
                                   VerticalAlignment="Center"
                                   RenderOptions.BitmapScalingMode="LowQuality"
                                   Source="{Binding Address,
                                                    IsAsync=True,
                                                    Converter={StaticResource GravatarImageConverter},
                                                    ConverterParameter=16}" />
                            <TextBlock Margin="2,0,2,0"
                                       Background="Transparent"
                                       FontWeight="Regular"
                                       Text="{Binding Name}" />
                        </StackPanel>

                    </Grid>
                </DataTemplate>
            </ui:AutoCompleteBox.TokenTemplate>
            <ui:AutoCompleteBox.ItemTemplate>
                <DataTemplate DataType="paranoia:MailContactContext">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto" />
                            <ColumnDefinition Width="*" />
                        </Grid.ColumnDefinitions>
                        <Image Grid.Column="0"
                               Width="40"
                               Margin="2,2,12,2"
                               HorizontalAlignment="Stretch"
                               RenderOptions.BitmapScalingMode="Fant"
                               Source="{Binding Address,
                                                IsAsync=True,
                                                Converter={StaticResource GravatarImageConverter}}"
                               Stretch="Uniform" />
                        <StackPanel Grid.Column="1" VerticalAlignment="Center">
                            <TextBlock FontSize="{DynamicResource {ComponentResourceKey {x:Type properties:Resources},
                                                                                        AppMediumLargeFontSize}}"
                                       FontWeight="Thin"
                                       Foreground="{DynamicResource {ComponentResourceKey {x:Type properties:Resources},
                                                                                          AppLightTextBrush}}"
                                       Text="{Binding Name}" />
                            <TextBlock FontWeight="Thin"
                                       Foreground="{DynamicResource {ComponentResourceKey {x:Type properties:Resources},
                                                                                          AppMediumTextBrush}}"
                                       Text="{Binding Address}" />
                        </StackPanel>
                    </Grid>
                </DataTemplate>
            </ui:AutoCompleteBox.ItemTemplate>
        </ui:AutoCompleteBox>
        <ui:HtmlControl Grid.Row="2"
                        Grid.Column="0"
                        Grid.ColumnSpan="2"
                        HorizontalAlignment="Stretch"
                        VerticalAlignment="Stretch"
                        Background="{DynamicResource {ComponentResourceKey {x:Type properties:Resources},
                                                                           AppControlBackgroundBrush}}"
                        IsTransparent="True"
                        Source="{Binding Text,
                                         Mode=TwoWay}"
                        TabIndex="2" />
        <Button Grid.Row="3"
                Grid.Column="0"
                HorizontalAlignment="Left"
                Style="{DynamicResource {ComponentResourceKey {x:Type properties:Resources},
                                                              AppCircleButtonStyle}}"
                TabIndex="3">
            <Image Width="20" Source="/Assets/send.png" />
        </Button>
    </Grid>
</Page>
