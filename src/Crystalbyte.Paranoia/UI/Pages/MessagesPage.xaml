<Page x:Class="Crystalbyte.Paranoia.UI.Pages.MessagesPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:componentModel="clr-namespace:System.ComponentModel;assembly=WindowsBase"
      xmlns:converters="clr-namespace:Crystalbyte.Paranoia.UI.Converters"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      xmlns:paranoia="clr-namespace:Crystalbyte.Paranoia"
      xmlns:properties="clr-namespace:Crystalbyte.Paranoia.Properties"
      xmlns:ui="clr-namespace:Crystalbyte.Paranoia.UI"
      Title="Messages"
      d:DataContext="{d:DesignInstance paranoia:AppContext}"
      d:DesignHeight="553.714"
      d:DesignWidth="1072.625"
      mc:Ignorable="d">
    <Page.Resources>
        <converters:BooleanToBrushConverter x:Key="SubjectBrushConverter"
                                            BrushForFalse="{StaticResource Metro.NormalTextBrush}"
                                            BrushForTrue="{StaticResource Metro.NormalTextBrush}" />
        <CollectionViewSource x:Key="MessagesSource" Source="{Binding Messages}">
            <CollectionViewSource.SortDescriptions>
                <componentModel:SortDescription Direction="Descending" PropertyName="EntryDate" />
            </CollectionViewSource.SortDescriptions>
        </CollectionViewSource>
    </Page.Resources>
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="320"
                              MinWidth="150"
                              MaxWidth="500" />
            <ColumnDefinition Width="Auto" />
            <ColumnDefinition Width="400"
                              MinWidth="150"
                              MaxWidth="800" />
            <ColumnDefinition Width="Auto" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <Grid Grid.Column="0" Background="{DynamicResource Metro.AlternateControlBackgroundBrush}">
            <DockPanel LastChildFill="True">
                <Border HorizontalAlignment="Stretch"
                        BorderBrush="{DynamicResource Metro.ControlBorderBrush}"
                        BorderThickness="0,0,0,1"
                        DockPanel.Dock="Top"
                        Padding="0,3"
                        SnapsToDevicePixels="True">
                    <Grid HorizontalAlignment="Left">
                        <ui:MetroCircleButton Margin="12"
                                              Command="ui:MessageCommands.Compose"
                                              Text="{x:Static properties:Resources.ComposeMessage}">
                            <Image Width="18" Source="/Assets/paper.png" />
                        </ui:MetroCircleButton>
                    </Grid>
                </Border>
                <DockPanel Margin="1"
                           DockPanel.Dock="Top"
                           LastChildFill="False">
                    <ui:MetroCircleButton Margin="6"
                                          HorizontalAlignment="Center"
                                          VerticalAlignment="Center"
                                          Command="{Binding CreateAccountCommand}"
                                          DockPanel.Dock="Right">
                        <Image Width="18" Source="/Assets/add.png" />
                    </ui:MetroCircleButton>
                    <TextBlock Margin="12,0,0,0"
                               VerticalAlignment="Center"
                               DockPanel.Dock="Left"
                               FontSize="{DynamicResource Metro.ExtraLargeFontSize}"
                               Style="{DynamicResource Metro.TitleTextStyle}"><Run Text="{x:Static properties:Resources.AccountsTitle}" /><Run Text=" " /><Run Foreground="{DynamicResource Metro.SecondaryTextBrush}" Text="{Binding Accounts, Converter={StaticResource CollectionCounter}, FallbackValue=0, Mode=OneWay, StringFormat=\{0\}}" />
                    </TextBlock>
                </DockPanel>
                <TreeView Margin="0"
                          HorizontalAlignment="Stretch"
                          VerticalAlignment="Stretch"
                          Background="{DynamicResource Metro.AlternateControlBackgroundBrush}"
                          BorderThickness="0"
                          ItemsSource="{Binding Accounts}"
                          Padding="0"
                          ScrollViewer.CanContentScroll="False"
                          SelectedItemChanged="OnTreeViewSelectedItemChanged"
                          VirtualizingPanel.IsVirtualizing="False"
                          VirtualizingPanel.ScrollUnit="Pixel">
                    <TreeView.Resources>
                        <DataTemplate DataType="{x:Type paranoia:OutboxContext}">
                            <StackPanel Orientation="Horizontal">
                                <Image Width="16"
                                       Margin="0,1,6,1"
                                       SnapsToDevicePixels="True"
                                       Source="/Assets/folder.png" />
                                <TextBlock x:Name="MailboxNameBlock"
                                           VerticalAlignment="Center"
                                           FontSize="{DynamicResource Metro.SmallFontSize}"
                                           Foreground="{DynamicResource Metro.NormalTextBrush}"
                                           Text="{x:Static properties:Resources.Outbox}" />
                            </StackPanel>
                            <DataTemplate.Triggers>
                                <DataTrigger Binding="{Binding IsSelected}" Value="True">
                                    <Setter TargetName="MailboxNameBlock" Property="Foreground" Value="{DynamicResource Metro.NormalTextBrush}" />
                                </DataTrigger>
                            </DataTemplate.Triggers>
                        </DataTemplate>
                        <HierarchicalDataTemplate DataType="{x:Type paranoia:MailAccountContext}" ItemsSource="{Binding Children, Mode=OneWay}">
                            <Grid>
                                <Grid.ContextMenu>
                                    <ContextMenu>
                                        <MenuItem Command="{Binding ShowUnsubscribedMailboxesCommand}" Visibility="{Binding IsManagingMailboxes, ConverterParameter=!, Converter={StaticResource BooleanToVisibilityConverter}}">
                                            <MenuItem.Icon>
                                                <Image Height="16" Source="/Assets/select.png" />
                                            </MenuItem.Icon>
                                            <MenuItem.Header>
                                                <TextBlock Foreground="{StaticResource Metro.NormalTextBrush}"
                                                           Style="{StaticResource Metro.MenuHeaderTextStyle}"
                                                           Text="{x:Static properties:Resources.ShowUnsubscribedMailboxes}" />
                                            </MenuItem.Header>
                                        </MenuItem>
                                        <MenuItem Command="{Binding HideUnsubscribedMailboxesCommand}" Visibility="{Binding IsManagingMailboxes, Converter={StaticResource BooleanToVisibilityConverter}}">
                                            <MenuItem.Icon>
                                                <Image Height="16" Source="/Assets/ok.png" />
                                            </MenuItem.Icon>
                                            <MenuItem.Header>
                                                <TextBlock Foreground="{StaticResource Metro.NormalTextBrush}"
                                                           Style="{StaticResource Metro.MenuHeaderTextStyle}"
                                                           Text="{x:Static properties:Resources.HideUnsubscribedMailboxes}" />
                                            </MenuItem.Header>
                                        </MenuItem>
                                        <Separator Visibility="{Binding IsManagingMailboxes, ConverterParameter=!, Converter={StaticResource BooleanToVisibilityConverter}}" />
                                        <MenuItem Command="{Binding DeleteAccountCommand}" Visibility="{Binding IsManagingMailboxes, ConverterParameter=!, Converter={StaticResource BooleanToVisibilityConverter}}">
                                            <MenuItem.Icon>
                                                <Rectangle Width="16"
                                                           Height="16"
                                                           Fill="{DynamicResource Metro.FailureBrush}">
                                                    <Rectangle.OpacityMask>
                                                        <ImageBrush ImageSource="/Assets/delete.png" />
                                                    </Rectangle.OpacityMask>
                                                </Rectangle>
                                            </MenuItem.Icon>
                                            <MenuItem.Header>
                                                <TextBlock Foreground="{StaticResource Metro.NormalTextBrush}"
                                                           Style="{StaticResource Metro.MenuHeaderTextStyle}"
                                                           Text="{x:Static properties:Resources.Delete}" />
                                            </MenuItem.Header>
                                        </MenuItem>
                                        <Separator Visibility="{Binding IsManagingMailboxes, ConverterParameter=!, Converter={StaticResource BooleanToVisibilityConverter}}" />
                                        <MenuItem Command="{Binding RegisterCommand}" Visibility="{Binding IsManagingMailboxes, ConverterParameter=!, Converter={StaticResource BooleanToVisibilityConverter}}">
                                            <!--
                                                <MenuItem.Icon>
                                                <Image Height="16" Source="/Assets/edit.properties.png" />
                                                </MenuItem.Icon>
                                            -->
                                            <MenuItem.Header>
                                                <TextBlock Foreground="{StaticResource Metro.NormalTextBrush}"
                                                           Style="{StaticResource Metro.MenuHeaderTextStyle}"
                                                           Text="{x:Static properties:Resources.Register}" />
                                            </MenuItem.Header>
                                        </MenuItem>
                                        <MenuItem Command="{Binding ConfigAccountCommand}" Visibility="{Binding IsManagingMailboxes, ConverterParameter=!, Converter={StaticResource BooleanToVisibilityConverter}}">
                                            <MenuItem.Icon>
                                                <Image Height="16" Source="/Assets/edit.properties.png" />
                                            </MenuItem.Icon>
                                            <MenuItem.Header>
                                                <TextBlock Foreground="{StaticResource Metro.NormalTextBrush}"
                                                           Style="{StaticResource Metro.MenuHeaderTextStyle}"
                                                           Text="{x:Static properties:Resources.Properties}" />
                                            </MenuItem.Header>
                                        </MenuItem>
                                    </ContextMenu>
                                </Grid.ContextMenu>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto" />
                                    <ColumnDefinition Width="*" />
                                    <ColumnDefinition Width="Auto" />
                                </Grid.ColumnDefinitions>
                                <Image Grid.Row="0"
                                       Grid.Column="0"
                                       Width="36"
                                       Margin="0,0,6,0"
                                       VerticalAlignment="Center"
                                       RenderOptions.BitmapScalingMode="Fant"
                                       Source="{Binding Address,
                                                        ConverterParameter=48,
                                                        Converter={StaticResource GravatarImageConverter}}" />
                                <StackPanel Grid.Column="1" VerticalAlignment="Center">
                                    <TextBlock Margin="2,-2,0,0"
                                               FontSize="{DynamicResource Metro.MediumLargeFontSize}"
                                               FontWeight="Thin"
                                               Foreground="{DynamicResource Metro.NormalTextBrush}"
                                               RenderOptions.ClearTypeHint="Enabled"
                                               Text="{Binding Name,
                                                              ConverterParameter=...,
                                                              Converter={StaticResource NullOrEmptyFormatter}}"
                                               TextTrimming="CharacterEllipsis"
                                               TextWrapping="NoWrap" />
                                    <TextBlock Margin="2,0,2,2"
                                               RenderOptions.ClearTypeHint="Enabled"
                                               Text="{Binding Address,
                                                              Converter={StaticResource NullOrEmptyFormatter}}"
                                               TextTrimming="CharacterEllipsis"
                                               TextWrapping="NoWrap">
                                        <TextBlock.Foreground>
                                            <Binding Mode="OneWay" Path="IsSelected">
                                                <Binding.Converter>
                                                    <converters:BooleanToBrushConverter BrushForFalse="{DynamicResource Metro.SecondaryTextBrush}" BrushForTrue="{DynamicResource Metro.NormalTextBrush}" />
                                                </Binding.Converter>
                                            </Binding>
                                        </TextBlock.Foreground>
                                    </TextBlock>
                                </StackPanel>
                            </Grid>
                        </HierarchicalDataTemplate>
                        <HierarchicalDataTemplate DataType="{x:Type paranoia:MailboxContext}" ItemsSource="{Binding Children}">
                            <Grid>
                                <Grid.ContextMenu>
                                    <ContextMenu>
                                        <MenuItem Command="{Binding SyncMailboxCommand}">
                                            <MenuItem.Icon>
                                                <Image Height="16" Source="/Assets/refresh.png" />
                                            </MenuItem.Icon>
                                            <MenuItem.Header>
                                                <TextBlock Style="{StaticResource Metro.MenuHeaderTextStyle}" Text="{x:Static properties:Resources.Sync}" />
                                            </MenuItem.Header>
                                        </MenuItem>
                                        <Separator />
                                        <MenuItem Command="{Binding CreateMailboxCommand}">
                                            <MenuItem.Icon>
                                                <Image Height="16"
                                                       RenderOptions.BitmapScalingMode="NearestNeighbor"
                                                       Source="/Assets/add.png"
                                                       UseLayoutRounding="True" />
                                            </MenuItem.Icon>
                                            <MenuItem.Header>
                                                <TextBlock Style="{StaticResource Metro.MenuHeaderTextStyle}" Text="{x:Static properties:Resources.New}" />
                                            </MenuItem.Header>
                                        </MenuItem>
                                        <MenuItem Command="{Binding DeleteMailboxCommand}">
                                            <MenuItem.Icon>
                                                <Image Height="16" Source="/Assets/delete.png" />
                                            </MenuItem.Icon>
                                            <MenuItem.Header>
                                                <TextBlock Style="{StaticResource Metro.MenuHeaderTextStyle}" Text="{x:Static properties:Resources.Delete}" />
                                            </MenuItem.Header>
                                        </MenuItem>
                                    </ContextMenu>
                                </Grid.ContextMenu>
                                <Rectangle HorizontalAlignment="Stretch"
                                           VerticalAlignment="Stretch"
                                           Fill="Transparent" />
                                <DockPanel HorizontalAlignment="Stretch" LastChildFill="False">
                                    <Border DockPanel.Dock="Left" Visibility="{Binding IsEditing, Converter={StaticResource BooleanToVisibilityConverter}}">
                                        <CheckBox VerticalAlignment="Center"
                                                  IsChecked="{Binding IsSubscribed,
                                                                      Mode=TwoWay}"
                                                  Visibility="{Binding IsSelectable,
                                                                       Converter={StaticResource BooleanToVisibilityConverter}}" />
                                    </Border>
                                    <Image Width="16"
                                           Margin="0,1,6,1"
                                           DockPanel.Dock="Left"
                                           SnapsToDevicePixels="True"
                                           Source="/Assets/folder.png" />
                                    <TextBlock x:Name="MailboxNameBlock"
                                               VerticalAlignment="Center"
                                               DockPanel.Dock="Left"
                                               FontSize="{DynamicResource Metro.SmallFontSize}"
                                               Text="{Binding LocalName}">
                                        <TextBlock.Foreground>
                                            <Binding Path="IsSubscribedAndSelectable">
                                                <Binding.Converter>
                                                    <converters:BooleanToBrushConverter BrushForFalse="{DynamicResource Metro.SecondaryTextBrush}" BrushForTrue="{DynamicResource Metro.NormalTextBrush}" />
                                                </Binding.Converter>
                                            </Binding>
                                        </TextBlock.Foreground>
                                    </TextBlock>
                                    <Border Margin="12,0"
                                            DockPanel.Dock="Right"
                                            Visibility="{Binding NotSeenCount,
                                                                 Converter={StaticResource QuantityToVisibilityConverter}}">
                                        <TextBlock VerticalAlignment="Center"
                                                   FontSize="{DynamicResource Metro.SmallFontSize}"
                                                   FontWeight="Bold"
                                                   Text="{Binding NotSeenCount}">
                                            <TextBlock.Foreground>
                                                <Binding Path="IsSelected">
                                                    <Binding.Converter>
                                                        <converters:BooleanToBrushConverter BrushForFalse="{DynamicResource Metro.App.AccentBrush}" BrushForTrue="{DynamicResource Metro.NormalTextBrush}" />
                                                    </Binding.Converter>
                                                </Binding>
                                            </TextBlock.Foreground>
                                        </TextBlock>
                                    </Border>
                                </DockPanel>
                                <ui:MetroProgressBar x:Name="EnvelopeProgressBar"
                                                     Height="2"
                                                     Margin="-22,0,-2.4,-3"
                                                     HorizontalAlignment="Stretch"
                                                     VerticalAlignment="Bottom"
                                                     Background="Transparent"
                                                     Maximum="{Binding TotalEnvelopeCount}"
                                                     Minimum="0"
                                                     Visibility="{Binding Value,
                                                                          Converter={StaticResource QuantityToVisibilityConverter},
                                                                          RelativeSource={RelativeSource Self}}"
                                                     Value="{Binding FetchedEnvelopeCount}" />
                            </Grid>
                            <HierarchicalDataTemplate.Triggers>
                                <DataTrigger Binding="{Binding IsSelected}" Value="True">
                                    <Setter TargetName="MailboxNameBlock" Property="Foreground" Value="{DynamicResource Metro.NormalTextBrush}" />
                                    <Setter TargetName="EnvelopeProgressBar" Property="Foreground" Value="{DynamicResource Metro.NormalTextBrush}" />
                                </DataTrigger>
                            </HierarchicalDataTemplate.Triggers>
                        </HierarchicalDataTemplate>
                    </TreeView.Resources>
                    <TreeView.ItemContainerStyle>
                        <Style BasedOn="{StaticResource {x:Type TreeViewItem}}" TargetType="{x:Type TreeViewItem}">
                            <!--  ReSharper disable Xaml.BindingWithContextNotResolved  -->
                            <Setter Property="IsSelected" Value="{Binding IsSelected, Mode=TwoWay}" />
                            <Setter Property="IsExpanded" Value="{Binding IsExpanded, Mode=TwoWay}" />
                            <Setter Property="Visibility" Value="{Binding IsListed, Converter={StaticResource BooleanToVisibilityConverter}, Mode=OneWay}" />
                            <!--  ReSharper restore Xaml.BindingWithContextNotResolved  -->
                        </Style>
                    </TreeView.ItemContainerStyle>
                </TreeView>
            </DockPanel>
        </Grid>
        <DockPanel Grid.Row="0"
                   Grid.Column="2"
                   Background="{DynamicResource Metro.AlternateControlBackgroundBrush}"
                   LastChildFill="True">
            <Grid DockPanel.Dock="Top">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>
                <TextBlock Margin="12,3,12,3"
                           VerticalAlignment="Center"
                           FontSize="{DynamicResource Metro.ExtraLargeFontSize}"
                           Style="{DynamicResource Metro.TitleTextStyle}">
                    <Run Text="{x:Static properties:Resources.MessagesTitle}" /><Run Text=" " /><Run Foreground="{DynamicResource Metro.SecondaryTextBrush}" Text="{Binding Messages, Converter={StaticResource CollectionCounter}, Mode=OneWay, StringFormat=\{0\}}" />
                </TextBlock>
                <TextBox Grid.Row="1"
                         Margin="6"
                         Background="{DynamicResource Metro.ControlBackgroundBrush}"
                         Text="{Binding QueryString,
                                        Mode=TwoWay,
                                        UpdateSourceTrigger=PropertyChanged}" />
                <DockPanel Grid.Row="2"
                           Margin="12,0,12,0"
                           HorizontalAlignment="Stretch"
                           LastChildFill="False">
                    <RadioButton Margin="3,3,9,3"
                                 Content="{x:Static properties:Resources.All}"
                                 DockPanel.Dock="Left"
                                 GroupName="Filter"
                                 IsChecked="{Binding ShowAllMessages,
                                                     Mode=TwoWay}"
                                 Style="{DynamicResource Metro.TextRadioButtonStyle}" />
                    <RadioButton Content="{x:Static properties:Resources.Unseen}"
                                 DockPanel.Dock="Left"
                                 GroupName="Filter"
                                 Style="{DynamicResource Metro.TextRadioButtonStyle}" />
                    <ToggleButton DockPanel.Dock="Right"
                                  IsChecked="{Binding IsSortAscending,
                                                      Mode=TwoWay}"
                                  Style="{DynamicResource Metro.SortingToggleButtonStyle}" />
                </DockPanel>
            </Grid>
            <Grid>
                <ListView x:Name="MessagesListView"
                          HorizontalContentAlignment="Stretch"
                          Panel.ZIndex="0"
                          Background="Transparent"
                          BorderThickness="0"
                          KeyboardNavigation.DirectionalNavigation="Contained"
                          KeyboardNavigation.TabNavigation="None"
                          Padding="0"
                          SelectionChanged="OnMessageSelectionChanged"
                          TabIndex="4"
                          VirtualizingPanel.IsVirtualizing="True"
                          VirtualizingPanel.VirtualizationMode="Recycling">
                    <ListView.Resources>
                        <converters:BooleanToBrushConverter x:Key="SeenBrushConverter"
                                                            BrushForFalse="{DynamicResource Metro.App.AccentBrush}"
                                                            BrushForTrue="{DynamicResource Metro.SecondaryTextBrush}" />
                    </ListView.Resources>
                    <ListView.ItemsSource>
                        <Binding Source="{StaticResource MessagesSource}" />
                    </ListView.ItemsSource>
                    <ListView.InputBindings>
                        <KeyBinding Key="R"
                                    Command="ui:MessageCommands.Reply"
                                    Modifiers="Control" />
                        <KeyBinding Key="F"
                                    Command="ui:MessageCommands.Forward"
                                    Modifiers="Control" />
                        <KeyBinding Key="Delete" Command="{Binding DeleteMessagesCommand}" />
                        <KeyBinding Key="D"
                                    Command="{Binding DeleteMessagesCommand}"
                                    Modifiers="Control" />
                    </ListView.InputBindings>
                    <ListView.ContextMenu>
                        <ContextMenu>
                            <MenuItem Command="ui:MessageCommands.Reply" InputGestureText="Ctrl + R">
                                <MenuItem.Icon>
                                    <Image Height="16"
                                           Source="/Assets/reply.png"
                                           UseLayoutRounding="True" />
                                </MenuItem.Icon>
                                <MenuItem.Header>
                                    <TextBlock Style="{StaticResource Metro.MenuHeaderTextStyle}" Text="{x:Static properties:Resources.Reply}" />
                                </MenuItem.Header>
                            </MenuItem>
                            <MenuItem Command="ui:MessageCommands.Forward" InputGestureText="Ctrl + F">
                                <MenuItem.Icon>
                                    <Image Height="16"
                                           Source="/Assets/forward.png"
                                           UseLayoutRounding="True" />
                                </MenuItem.Icon>
                                <MenuItem.Header>
                                    <TextBlock Style="{StaticResource Metro.MenuHeaderTextStyle}" Text="{x:Static properties:Resources.Forward}" />
                                </MenuItem.Header>
                            </MenuItem>
                            <MenuItem Command="{Binding RestoreMessagesCommand}" InputGestureText="Ctrl + Alt + R">
                                <MenuItem.Icon>
                                    <Image Height="16"
                                           Source="/Assets/message.restore.png"
                                           UseLayoutRounding="True" />
                                </MenuItem.Icon>
                                <MenuItem.Header>
                                    <TextBlock Style="{StaticResource Metro.MenuHeaderTextStyle}" Text="{x:Static properties:Resources.Restore}" />
                                </MenuItem.Header>
                            </MenuItem>
                            <Separator />
                            <MenuItem Command="{Binding DeleteMessagesCommand}" InputGestureText="Entf">
                                <MenuItem.Icon>
                                    <Rectangle Width="16"
                                               Height="16"
                                               Fill="{DynamicResource Metro.FailureBrush}">
                                        <Rectangle.OpacityMask>
                                            <ImageBrush ImageSource="/Assets/delete.png" />
                                        </Rectangle.OpacityMask>
                                    </Rectangle>
                                </MenuItem.Icon>
                                <MenuItem.Header>
                                    <TextBlock Style="{StaticResource Metro.MenuHeaderTextStyle}" Text="{x:Static properties:Resources.Delete}" />
                                </MenuItem.Header>
                            </MenuItem>
                            <Separator />
                            <MenuItem>
                                <MenuItem.Header>
                                    <TextBlock Style="{StaticResource Metro.MenuHeaderTextStyle}" Text="{x:Static properties:Resources.Advanced}" />
                                </MenuItem.Header>
                                <MenuItem Command="{Binding MarkAsFlaggedCommand}">
                                    <MenuItem.Icon>
                                        <Rectangle Width="16"
                                                   Height="16"
                                                   Fill="{DynamicResource Metro.WarningColor}">
                                            <Rectangle.OpacityMask>
                                                <ImageBrush ImageSource="/Assets/star.full.png" />
                                            </Rectangle.OpacityMask>
                                        </Rectangle>
                                    </MenuItem.Icon>
                                    <MenuItem.Header>
                                        <TextBlock Style="{StaticResource Metro.MenuHeaderTextStyle}" Text="{x:Static properties:Resources.MarkAsFlagged}" />
                                    </MenuItem.Header>
                                </MenuItem>
                                <MenuItem Command="{Binding MarkAsNotFlaggedCommand}">
                                    <MenuItem.Icon>
                                        <Image Height="16"
                                               Source="/Assets/star.png"
                                               UseLayoutRounding="True" />
                                    </MenuItem.Icon>
                                    <MenuItem.Header>
                                        <TextBlock Style="{StaticResource Metro.MenuHeaderTextStyle}" Text="{x:Static properties:Resources.MarkAsNotFlagged}" />
                                    </MenuItem.Header>
                                </MenuItem>
                                <Separator />
                                <MenuItem Command="{Binding MarkAsSeenCommand}">
                                    <MenuItem.Icon>
                                        <Image Height="16"
                                               Source="/Assets/seen.png"
                                               UseLayoutRounding="True" />
                                    </MenuItem.Icon>
                                    <MenuItem.Header>
                                        <TextBlock Style="{StaticResource Metro.MenuHeaderTextStyle}" Text="{x:Static properties:Resources.MarkAsSeen}" />
                                    </MenuItem.Header>
                                </MenuItem>
                                <MenuItem Command="{Binding MarkAsNotSeenCommand}">
                                    <MenuItem.Icon>
                                        <Image Height="16"
                                               Source="/Assets/message.png"
                                               UseLayoutRounding="True" />
                                    </MenuItem.Icon>
                                    <MenuItem.Header>
                                        <TextBlock Style="{StaticResource Metro.MenuHeaderTextStyle}" Text="{x:Static properties:Resources.MarkAsNotSeen}" />
                                    </MenuItem.Header>
                                </MenuItem>
                            </MenuItem>
                        </ContextMenu>
                    </ListView.ContextMenu>
                    <ListView.ItemsPanel>
                        <ItemsPanelTemplate>
                            <VirtualizingStackPanel x:Name="Mvsp" />
                        </ItemsPanelTemplate>
                    </ListView.ItemsPanel>
                    <ListView.ItemContainerStyle>
                        <Style BasedOn="{StaticResource {x:Type ListViewItem}}" TargetType="{x:Type ListViewItem}">
                            <!--  ReSharper disable once Xaml.BindingWithContextNotResolved  -->
                            <Setter Property="IsSelected" Value="{Binding IsSelected, Mode=OneWayToSource}" />
                            <Setter Property="Width" Value="{Binding ActualWidth, ElementName=Mvsp}" />
                            <Setter Property="Padding" Value="6,1,6,1" />
                        </Style>
                    </ListView.ItemContainerStyle>
                    <ListView.ItemTemplate>
                        <DataTemplate DataType="paranoia:MailMessageContext">
                            <Grid>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="24" />
                                    <RowDefinition Height="22" />
                                </Grid.RowDefinitions>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto" />
                                    <ColumnDefinition Width="*" />
                                    <ColumnDefinition Width="Auto" />
                                </Grid.ColumnDefinitions>
                                <Rectangle Grid.Row="0"
                                           Grid.RowSpan="2"
                                           Grid.Column="0"
                                           Grid.ColumnSpan="3"
                                           Fill="Transparent" />
                                <Image Grid.Row="0"
                                       Grid.RowSpan="2"
                                       Grid.Column="0"
                                       Width="34"
                                       Height="34"
                                       Margin="6,0,12,0"
                                       VerticalAlignment="Center"
                                       Source="{Binding FromAddress,
                                                        ConverterParameter=48,
                                                        Converter={StaticResource GravatarImageConverter},
                                                        IsAsync=True}" />

                                <TextBlock x:Name="SubjectBlock"
                                           Grid.Row="1"
                                           Grid.Column="1"
                                           Margin="0,0,6,0"
                                           VerticalAlignment="Center"
                                           FontSize="{DynamicResource Metro.NormalFontSize}"
                                           Foreground="{Binding IsSeen,
                                                                Converter={StaticResource SeenBrushConverter}}"
                                           Text="{Binding Subject,
                                                          ConverterParameter=...,
                                                          Converter={StaticResource NullOrEmptyFormatter}}"
                                           TextTrimming="CharacterEllipsis"
                                           TextWrapping="NoWrap" />
                                <StackPanel Grid.Row="0"
                                            Grid.Column="2"
                                            Margin="0,1,3,0"
                                            HorizontalAlignment="Right"
                                            VerticalAlignment="Center"
                                            Orientation="Horizontal">
                                    <Rectangle Width="16"
                                               Height="16"
                                               Fill="{DynamicResource Metro.WarningColor}"
                                               Visibility="{Binding IsFlagged,
                                                                    Converter={StaticResource BooleanToVisibilityConverter}}">
                                        <Rectangle.OpacityMask>
                                            <ImageBrush ImageSource="/Assets/star.full.png" />
                                        </Rectangle.OpacityMask>
                                    </Rectangle>
                                </StackPanel>
                                <TextBlock x:Name="FromBlock"
                                           Grid.Row="0"
                                           Grid.Column="1"
                                           Margin="0,0,12,0"
                                           HorizontalAlignment="Left"
                                           VerticalAlignment="Center"
                                           FontSize="{DynamicResource Metro.MediumLargeFontSize}"
                                           FontWeight="Thin"
                                           Foreground="{Binding IsSubjectNilOrEmpty,
                                                                Converter={StaticResource SubjectBrushConverter}}"
                                           Text="{Binding Converter={StaticResource MailContactFormatter}}"
                                           TextTrimming="CharacterEllipsis"
                                           TextWrapping="NoWrap" />

                                <TextBlock x:Name="DateBlock"
                                           Grid.Row="1"
                                           Grid.Column="2"
                                           Margin="0,0,6,0"
                                           HorizontalAlignment="Right"
                                           VerticalAlignment="Center"
                                           FontSize="{DynamicResource Metro.SmallFontSize}"
                                           Foreground="{DynamicResource Metro.SecondaryTextBrush}"
                                           Text="{Binding EntryDate,
                                                          Converter={StaticResource DateFormatter}}" />

                            </Grid>
                            <DataTemplate.Triggers>
                                <DataTrigger Binding="{Binding IsSelected}" Value="True">
                                    <Setter TargetName="SubjectBlock" Property="Foreground" Value="{DynamicResource Metro.NormalTextBrush}" />
                                    <Setter TargetName="DateBlock" Property="Foreground" Value="{DynamicResource Metro.NormalTextBrush}" />
                                </DataTrigger>
                            </DataTemplate.Triggers>
                        </DataTemplate>
                    </ListView.ItemTemplate>
                </ListView>
                <ui:ProgressRing Width="30"
                                 Height="30"
                                 HorizontalAlignment="Center"
                                 VerticalAlignment="Center"
                                 Panel.ZIndex="1"
                                 DataContext="{Binding SelectedMailbox}"
                                 IsActive="{Binding IsLoadingMessages,
                                                    Mode=OneWay}"
                                 IsLarge="False" />
                <TextBlock Margin="12"
                           HorizontalAlignment="Left"
                           VerticalAlignment="Top"
                           FontSize="{DynamicResource Metro.MediumFontSize}"
                           Foreground="{DynamicResource Metro.SecondaryTextBrush}"
                           Text="{x:Static properties:Resources.NoMessages}"
                           Visibility="{Binding MessageCount,
                                                ConverterParameter=!,
                                                Converter={StaticResource QuantityToVisibilityConverter}}" />
            </Grid>
        </DockPanel>
        <GridSplitter Grid.Row="0" Grid.Column="1" />
        <GridSplitter Grid.Row="0" Grid.Column="3" />
        <Grid Grid.Row="0"
              Grid.Column="4"
              Background="{DynamicResource Metro.ControlBackgroundBrush}">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>
            <Border Grid.Row="0" Padding="0,0,0,6">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="*" />
                    </Grid.RowDefinitions>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="*" />
                    </Grid.ColumnDefinitions>
                    <TextBlock Grid.Row="0"
                               Grid.Column="0"
                               Grid.ColumnSpan="2"
                               Margin="12,12,12,3"
                               DataContext="{Binding SelectedMessage}"
                               Style="{DynamicResource Metro.TitleTextStyle}"
                               Text="{Binding Subject,
                                              Converter={StaticResource NullOrEmptyFormatter},
                                              Mode=OneWay}"
                               TextTrimming="CharacterEllipsis"
                               TextWrapping="NoWrap" />
                    <TextBlock Grid.Row="1"
                               Grid.Column="0"
                               Margin="12,0,12,0"
                               DataContext="{Binding SelectedMessage}"
                               FontWeight="Normal"
                               Style="{DynamicResource Metro.SubtitleTextStyle}"
                               Text="{Binding FromName}" />
                    <TextBlock Grid.Row="2"
                               Grid.Column="0"
                               Margin="12,0,12,0"
                               DataContext="{Binding SelectedMessage}"
                               FontSize="{DynamicResource Metro.SmallFontSize}"
                               FontWeight="Normal"
                               Style="{DynamicResource Metro.SubtitleTextStyle}"
                               Text="{Binding EntryDate}" />
                    <StackPanel Grid.Row="1"
                                Grid.RowSpan="2"
                                Grid.Column="1"
                                Margin="6,0,6,0"
                                HorizontalAlignment="Right"
                                VerticalAlignment="Center"
                                Orientation="Horizontal"
                                Visibility="{Binding SelectedMessage,
                                                     Converter={StaticResource NullToVisibilityConverter}}">
                        <ui:MetroCircleButton BorderBrush="{DynamicResource Metro.NormalTextBrush}" Command="ui:MessageCommands.Reply">
                            <Image Width="18"
                                   Margin="-2,2,0,0"
                                   Source="/Assets/reply.png" />
                        </ui:MetroCircleButton>
                        <ui:MetroCircleButton BorderBrush="{DynamicResource Metro.NormalTextBrush}" Command="ui:MessageCommands.Forward">
                            <Image Width="18"
                                   Margin="-2,2,0,0"
                                   Source="/Assets/forward.png" />
                        </ui:MetroCircleButton>
                        <ui:MetroCircleButton BorderBrush="{DynamicResource Metro.NormalTextBrush}" Command="{Binding DeleteMessagesCommand}">
                            <Rectangle Width="18"
                                       Height="16"
                                       Fill="{DynamicResource Metro.FailureBrush}">
                                <Rectangle.OpacityMask>
                                    <ImageBrush ImageSource="/Assets/delete.png" />
                                </Rectangle.OpacityMask>
                            </Rectangle>
                        </ui:MetroCircleButton>
                        <ui:MetroCircleButton BorderBrush="{DynamicResource Metro.NormalTextBrush}" Command="ApplicationCommands.Print">
                            <Image Width="18"
                                   Margin="0,0,0,0"
                                   Source="/Assets/print.png" />
                        </ui:MetroCircleButton>
                    </StackPanel>
                    <ListView Grid.Row="3"
                              Grid.Column="0"
                              Grid.ColumnSpan="2"
                              MaxHeight="300"
                              Margin="0,3,0,0"
                              Background="{DynamicResource Metro.MenuBackgroundBrush}"
                              BorderThickness="0"
                              ItemsSource="{Binding Attachments}"
                              Padding="0,3,0,3"
                              ScrollViewer.HorizontalScrollBarVisibility="Disabled"
                              ScrollViewer.VerticalScrollBarVisibility="Auto">
                        <ListView.Style>
                            <Style BasedOn="{StaticResource {x:Type ListView}}" TargetType="{x:Type ListView}">
                                <EventSetter Event="MouseDoubleClick" Handler="OnAttachmentMouseDoubleClicked" />
                                <Style.Triggers>
                                    <Trigger Property="HasItems" Value="false">
                                        <Setter Property="Visibility" Value="Hidden" />
                                    </Trigger>
                                </Style.Triggers>
                            </Style>
                        </ListView.Style>
                        <ListView.ItemsPanel>
                            <ItemsPanelTemplate>
                                <WrapPanel Orientation="Horizontal" />
                            </ItemsPanelTemplate>
                        </ListView.ItemsPanel>
                        <ListView.ItemTemplate>
                            <DataTemplate>
                                <Border Padding="3">
                                    <Grid>
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="Auto" />
                                            <ColumnDefinition Width="120" />
                                        </Grid.ColumnDefinitions>
                                        <Image Grid.Column="0"
                                               Width="16"
                                               Margin="3,0,6,0"
                                               VerticalAlignment="Center"
                                               Source="{Binding FullName,
                                                                ConverterParameter=24,
                                                                Converter={StaticResource IconFinder},
                                                                IsAsync=True}"
                                               Stretch="Uniform" />
                                        <TextBlock Grid.Column="1"
                                                   Margin="0,0,6,0"
                                                   HorizontalAlignment="Left"
                                                   VerticalAlignment="Center"
                                                   Text="{Binding Name}"
                                                   TextTrimming="CharacterEllipsis"
                                                   TextWrapping="NoWrap"
                                                   ToolTip="{Binding FullName}" />

                                    </Grid>
                                </Border>
                            </DataTemplate>
                        </ListView.ItemTemplate>
                    </ListView>
                </Grid>
            </Border>
            <Grid Grid.Row="1"
                  HorizontalAlignment="Stretch"
                  VerticalAlignment="Stretch"
                  Visibility="{Binding IsAnimating,
                                       Converter={StaticResource BooleanToVisibilityConverter},
                                       ConverterParameter=!}">
                <Border Margin="0"
                        BorderThickness="0"
                        DataContext="{Binding SelectedMessage}"
                        Visibility="{Binding IsNotLoading,
                                             Converter={StaticResource BooleanToVisibilityConverter}}">
                    <Border Visibility="{Binding Converter={StaticResource NullToVisibilityConverter}}">
                        <ui:HtmlControl x:Name="HtmlControl"
                                        Background="{DynamicResource Metro.ControlBackgroundBrush}"
                                        BorderThickness="0"
                                        DataContext="{Binding DataContext,
                                                              RelativeSource={RelativeSource FindAncestor,
                                                                                             AncestorType=Page}}"
                                        IsReadOnly="True"
                                        IsTransparent="False"
                                        SnapsToDevicePixels="True"
                                        Source="{Binding Source,
                                                         Mode=OneWay}"
                                        WebSession="{Binding Source={StaticResource InspectionWebSessionProvider}}"
                                        Zoom="{Binding Zoom,
                                                       Mode=TwoWay}"
                                        d:IsHidden="True" />
                    </Border>
                </Border>
                <Grid DataContext="{Binding SelectedMessage}" Visibility="{Binding IsLoading, Converter={StaticResource BooleanToVisibilityConverter}, FallbackValue=Collapsed}">
                    <StackPanel HorizontalAlignment="Stretch" VerticalAlignment="Top">
                        <ui:MetroProgressBar Height="1"
                                             Margin="0"
                                             HorizontalAlignment="Stretch"
                                             Background="Transparent"
                                             Maximum="100"
                                             Minimum="0"
                                             Value="{Binding ProgressChanged}" />


                    </StackPanel>
                    <TextBlock Margin="24,12"
                               HorizontalAlignment="Left"
                               VerticalAlignment="Bottom"
                               Foreground="{DynamicResource Metro.SecondaryTextBrush}"><Run FontSize="48"
                             FontWeight="Thin"
                             Foreground="{DynamicResource Metro.App.AccentBrush}"
                             Text="{Binding ProgressChanged}" /><Run Text=" " /><Run FontSize="{DynamicResource Metro.NormalFontSize}"
                             FontWeight="Thin"
                             Text="%" />
                    </TextBlock>
                </Grid>
            </Grid>
        </Grid>
    </Grid>
</Page>
